# 🚨 تحذير — كل أرقام الأداء قبل 2026-09-24 باطلة
## ✅ الآلة أُصلحت في L0046 · والرقم الحقيقي أدناه

**تاريخ الاكتشاف:** 2026-09-24 · **المرجع:** `docs/lanes/L0045-VERDICT-entry-batch2.md`

---

## الخلاصة في سطر
**آلة المحاكاة كانت تبيع بأسعار لم توجد في السوق.** كل رقم ربح نشره المشروع
قبل هذا التاريخ **مضخَّم أو معكوس**.

## موضع العطب
`history/hyp_lab/common.py` → الدالة `simulate._step`:

```python
if gain >= p["trig"]:                                  # التسليح عند +0.15%
    local = max(p["entry"] * (1 + p["lock"]), peak * (1 - trail))
    eff = max(eff, local)                              # eff = الدخول + 0.60%
if l[j] <= eff:
    _close_position(p, j, eff, "stop")                 # ← البيع عند eff
```

عند بلوغ الربح **+0.15%** يقفز الوقف إلى مستوى القفل **+0.60%**،
**دون اشتراط أن يكون السعر بلغ +0.60% فعلًا**. ثم يُغلق المركز عند ذلك المستوى.
⇒ **الآلة تطبع +0.60% لكل صفقة تتسلّح.**

## برهان رقمي (أُعيد التحقق منه مستقلًّا)
| البند | السعر |
|---|---|
| سعر الدخول | 100.1300 |
| مستوى القفل (دخول +0.60%) | 100.7308 |
| **أعلى سعر بلغه السوق فعلًا** | **100.4000** |
| **سعر البيع المسجَّل** | **100.7308** |

**بيع أعلى بـ 0.33 من أي سعر وُجد في السوق.** تعبئة مستحيلة.

## حجم الأثر (من L0045)
- **38.7%** من صفقات فترة الحكم خرجت عند **+0.60% بالضبط**.
- **18.6%** خرجت بسعر **أعلى من أي سعر** سُجِّل في نافذة الصفقة.
- الرقم المعلَن **5,749.34$** ⇒ بالقياس الصحيح **−1,755.67$**
  (وبمسار الدقيقة الحقيقي −1,882.43$ · تطابق ضمن ~7%).
- الشراء والاحتفاظ بنفس آلة الخروج يعطي **7,284.79$** — أكثر من المحفظة كلها
  ⇒ «الربح» مصدره العطب لا اختيار الدخول.

## ما الذي بطل
**كل التقارير التالية أرقامها غير صالحة** (المنهج قد يبقى صالحًا، الأرقام لا):
`L0007` · `L0009` · `L0010` · `L0012` · `L0013` · `L0014` · `L0015` · `L0017` ·
`L0018` · `L0019` · `L0020` · `L0021` · `L0022` · `L0023` · `L0024` · `L0025` ·
`L0027` · `L0032` · `L0033` · `L0034` · `L0035` · `L0036` · `L0037` · `L0038` ·
`L0039` · `L0040` · `L0041` · `L0042` · `L0043` · `L0044`

**ولا تُقرأ الأحكام المبنية عليها** — ومنها:
- «تركيبة الخروج تضيف +320$» — مبنية على الآلة المعطوبة.
- «قانون الكثرة: كل تقليل للصفقات يخسر» — **غير قابل للتقييم**؛ الآلة كانت
  تمنح كل صفقة ربحًا مضمونًا، فالكثرة تربح بالتعريف. القانون يحتاج إعادة اختبار.
- كل أرقام الأعمدة السبعة وترتيبها.

## الحالة الآن
🛑 **كل تطوير موقوف.** لا فرضيات جديدة ولا أعمدة ولا جولات تحسين
حتى تُصلَح الآلة ويُعاد قياس الأساس من الصفر.

## اعتراف الوكيل القائد (§4)
في جولة L0024 بنيتُ محرّك خروج موسّعًا و«عايرتُه» على هذه الآلة فتطابق حرفًا،
وقدّمتُ التطابق **دليل صحة**. كان في الحقيقة **دليل نسخ للعطب نفسه**.
المعايرة على مرجع معطوب لا تثبت شيئًا — **تُضاف هذه إلى بروتوكول الثقة.**

## الواجب قبل أي رقم جديد
1. إصلاح `simulate._step`: لا تعبئة فوق أعلى سعر متاح في الشمعة.
2. إعادة قياس الأساس من الصفر بالآلة المصلَحة.
3. إعادة الحكم على كل ما بُني على الأرقام الباطلة.
4. **السؤال المفتوح:** هل بقي للمشروع حافة حقيقية أصلًا؟ — لم يُقَس.

---

# ✅ تحديث L0046 — أُصلحت الآلة، وهذا الرقم الحقيقي

| البند | الرقم المقيس بالآلة المصلَحة |
|---|---|
| **المحفظة — فترة الحكم** | **−1,762.57$** (30,539 صفقة · PF 0.6764) |
| المحفظة — فترة الاختيار | −1,007.83$ |
| الشراء والاحتفاظ (لا تفعل شيئًا) | **−73.29$** ⇒ أفضل من البوت بـ **1,689.28$** |
| الرقم المعلَن سابقًا (معطوب) | +5,749.34$ ⇒ الوهم كان **7,511.91$** |

**الأعمدة السبعة كلها سالبة** في الفترتين بلا استثناء واحد.

## 🔬 التشخيص الجذري — لا حافة أصلًا
| الفترة | الصافي$ | الكلفة$ | **الربح قبل الكلفة$** |
|---|---|---|---|
| الاختيار | −1,007.83 | 1,063.39 | +55.56 |
| الحكم | −1,762.57 | 1,585.74 | **−176.83** |

**الربح قبل الكلفة ≈ صفر.** ⇒ المشكلة ليست في الإعدادات ولا في الخروج ولا في
اختيار العملات، بل في **غياب الحافة**: كل صفقة تحتاج أن تكسب **0.26%** لتغطي
كلفتها فقط، ولا إشارة من السبع تفعل ذلك على نحو مستدام.

## تقاطع أربع آلات مستقلة (فترة الحكم)
| الآلة | صافي$ |
|---|---|
| المعطوبة (القديمة) | +5,876.51 |
| L0045 الدفاعية | −1,755.67 |
| L0045 مسار الدقيقة | −1,882.43 |
| **L0046 المصلَحة** | **−1,762.57** |
| نموذج المحرك الحي | −1,391.40 |

## ⚖️ انقلاب «قانون الكثرة»
القانون القديم *«كل تقليل للصفقات يخسر»* استُخلص كله من الآلة المعطوبة ⇒ **باطل**.
بالآلة الصحيحة: فلتر المتوسط 200 قصّ الصفقات 53% و**خفّض الخسارة بـ969.84$**.
**لكنه لا يصنع ربحًا** (−792.73$ بعد الفلتر).

> **القانون الصحيح:** لا علاقة أصيلة بين عدد الصفقات والربح. العدد يضخّم الكلفة،
> والكلفة هي الخسارة. تقليل الصفقات يصغّر الخسارة — ولا يصنع ربحًا من غير حافة.

## 🛡️ لماذا مرّ العطب 30 جولة — وكيف أُغلقت الثغرة
**الكاناري كان يحرس المحرك الحي فقط، ولا يحرس آلة المختبر إطلاقًا.**
وكل «معايرة» كانت تقيس الآلة على نفسها.

حارسان جديدان يمنعان التكرار:
- `tools/fill_invariant_check.py` — يرفض أي تعبئة خارج نطاق الشمعة (6/6 حالات · صفر مخالفة على 169,264 صفقة).
- `tools/canary_lab.py` — كاناري آلة المختبر: أي تحريف في `common.py` ⇒ فشل فوري.

**قاعدة دائمة:** أي جولة تظهر فيها مخالفة تعبئة واحدة ⇒ تُوقف فورًا وتُصلح الآلة
قبل أي تفسير، ولا تُقرأ أرقامها إطلاقًا.

## الحالة
🛑 لا مال حقيقي · 🛑 لا دفعات أعمدة أو فرضيات جديدة.
🔬 السؤال الوحيد المتبقي: **هل توجد إشارة دخول تصنع أكثر من 0.26% قبل الكلفة؟**
إن كان الجواب لا ⇒ الحكم على **مقاربة الشراء الاتجاهي كلها**، لا على عمود بعينه.
