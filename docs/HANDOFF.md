# 🤝 وثيقة التسليم الحية — docs/HANDOFF.md

> **الغرض (الحكم الدستوري د-٣):** أي جلسة جديدة — أو المستخدم من تيرمكس — تستأنف من هنا بلا أي فقدان.
> تتحدث بعد كل إنجاز. آخر تحديث: 2026-09-16 (جلسة إعادة التنظيم الثانية).

## أين نحن الآن

- **الكنز محصى ومثبت:** الحزمة `nova_upload_bundle.tar.gz` (البصمة مطابقة) فُكّت وأُحصيت بالكامل:
  `new/CENSUS.md` — ٢٥٨ ملف كود من الكنز (صفر جروح) + أداتان + ١٧ ملفاً معزولاً بمفاتيح
  (الخريطة: `new/_quarantine/MAP.md` — لا تدخل git أبداً).
- **التاج مؤكد:** `new/NOVA_bin_pending/جميع اصدارات البوت/1/NOVA.Finally_X/NOVA.py`
  = V7.0.1 النقي (ast سليم · ٢٦٢ دالة · صفر جروح · فرقه عن نسخة المستودع = الجروح
  الأربعة فقط) — الإثبات الكامل: `new/CROWN-CHECK.md`.
- **خطة إعادة التنظيم جاهزة للاعتماد:** `docs/REORG-PLAN-2026-09-16.md`
  (المجلدات: live/ · history/ · bot/ · old/ + ٥ دفعات + ٤ نقاط قرار متبقية).

## القواعد الحديدية (مختصرة — التفصيل في الدستور)

1. لا دمج ولا دفع إلى main من أي وكيل (الحكم د-١) — الدمج من تيرمكس:
   `bash tools/merge_main.sh`
2. لا تحريك: nova_v8/ · tools/ · crypto_archive/ · data/ · ملفات الدستور في الجذر.
3. لا حذف مكررات إلا بعد إثبات البوت القائد وبأمر صريح.
4. لا ذكاء اصطناعي يقترب من البوت — كود Gemini في التاج يبقى معطلاً.
5. البيئة تُعاد تدويرها بين الجولات — كل ثمين يُلتزم فوراً، وإعادة بناء شجرة الفحص:
   `bash new/extract.sh`

## الخطوة التالية (بانتظار المستخدم)

1. اعتماد خطة إعادة التنظيم ونقاطها المتبقية.
2. قرار تدوير المفاتيح الثلاثة (المستودع عام والحزمة تحويها داخل الأكياس).
3. بعد الاعتماد: الدفعة ١ (الهيكل التاريخي) — بلا أي حذف.

## لاستئناف العمل (جلسة جديدة)

اقرأ بالترتيب: `CONSTITUTION.md` ثم `docs/CONSTITUTION-AMENDMENTS.md` (خصوصاً حكم الدمج د-١..د-٣)
ثم هذه الوثيقة ثم `docs/REORG-PLAN-2026-09-16.md`. الزم الدستور حرفياً.
