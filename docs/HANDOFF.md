# 🤝 وثيقة التسليم الحية — docs/HANDOFF.md

> **الغرض (الحكم الدستوري د-٣):** أي جلسة جديدة — أو المستخدم من تيرمكس — تستأنف من هنا بلا أي فقدان.
> تتحدث بعد كل إنجاز. آخر تحديث: 2026-09-16 (تنفيذ الدفعات ١–٤ من إعادة التنظيم).

## أين نحن الآن

- **الدفعة ١ منفَّذة ✅ (بموافقة المستخدم الفورية على الخطة بتوصياتها):** الهيكل التاريخي قائم —
  في `history/`: `bot_versions` · `archive` · `hypotheses` · `hyp_lab` · `research` ·
  `sweep_results` · `sweep_results_donchian_v2` · `library` (١٬٥٨١ ملفاً بـ`git mv` — صفر حذف)،
  وفي `old/`: `code_drafts`. الممنوع تحريكه لم يُمس (الخطة §٢-١).
- **الدفعة ٢ منفَّذة ✅ (القرار `D‑0022`):** `live/` قائم — ملف القواعد الحية منقول إليه + وثيقة ربط
  (`live/README.md`) · الحزمة الأم في `history/` · `fetch_archive.py` باقٍ في الجذر بعد مسح إشارات
  نظيف · سطر تعديل المسار في `docs/CONSTITUTION-AMENDMENTS.md`.
- **بوابة الإشارات موثقة:** القرار `D‑0021` — ثلاث أدوات حُدّثت مساراتها الحية
  (`tools/organize_incoming.py` · `tools/archive_report.py` · `tools/audit_lane.py`)
  والوثائق التاريخية في `docs/` لم تُمس (البند ٢٢) · `INDEX.md` محدّث للخريطة الجديدة.
- **تحقق فعلي:** الأدوات الثلاث تعمل من المسارات الجديدة (تدقيق ٣٦ فحصاً + جدول الأرشيف + خروج نظيف للمنظّم).
- **الدفعة ٣ منفَّذة ✅ (القرار `D‑0023`):** الكنز النقي الفريد — ٢٠٠ نسخة (٢٥.٩ ميغابايت) في
  `history/clean/` مع شهادة البصمات الكاملة `history/clean/MANIFEST-CLEAN.md`؛ المطابقة مع الإحصاء:
  صفر مفقود من بصمات الكنز الـ٢٥٨. المكررات (١٤٩) في `old/bundle_rest/` غير المتعقبة بالتصميم
  (اشتقاق ١٠٠٪ بأمر واحد). الاستخراج `new/` أُغلق — بقي المتعقَّب فقط + المعزل السري.
- **التاج في مكانه المعلوم بالنقي:**
  `history/clean/NOVA_bin_pending/جميع اصدارات البوت/1/NOVA.Finally_X/NOVA.py`
  = V7.0.1 النقي (٣٨٣٬٣٧٤ بايت · بصمة `3e08f1f8f103…`) — الإثبات التاريخي في `new/CROWN-CHECK.md`.
- **المعزل السري عاد إلى مكانه:** ١٧ ملف المفاتيح في `new/_quarantine/` (لا تدخل git أبداً —
  الخريطة `new/_quarantine/MAP.md`).

## القواعد الحديدية (مختصرة — التفصيل في الدستور)

1. لا دمج ولا طلب دمج ولا دفع إلى `main` من أي وكيل (الحكم د-١/د-٢) — الدمج من تيرمكس:
   `bash tools/merge_main.sh`
2. لا تحريك: `nova_v8/` · `tools/` (المجلد) · `crypto_archive/` · `data/` · ملفات الدستور في الجذر.
3. لا حذف مكررات إلا بعد إثبات البوت القائد وبأمر صريح.
4. لا ذكاء اصطناعي يقترب من البوت — كود Gemini في التاج يبقى معطلاً.
5. البيئة تُعاد تدويرها بين الجولات — كل ثمين يُلتزم فوراً، وشجرة الفحص تُبنى من المصدر المتعقب:
   `bash new/extract.sh`

## الخطوة التالية

1. **دفعة لاحقة — نقطة القرار ٣:** وثائق النتائج في `docs/` (lanes/ وسجلات التدقيق) بعد مسح إشارات.
2. **الدفعة ٥ — الحذف:** فقط بعد إثبات البوت القائد وبأمر صريح (لا تفتح قبله).
4. **معلق لدى المستخدم:** قرار تدوير المفاتيح الثلاثة (المستودع عام والحزمة تحويها داخل الأكياس)
   + تنفيذ الدمج من جهازه متى شاء: `bash tools/merge_main.sh`.

### سجل الدفعات المنفَّذة

- **الدفعة ١ ✅** الهيكل التاريخي (القراران `D‑0021` · `D‑0022` في سياقها) — ثماني مجموعات إلى
  `history/` والمسودات إلى `old/` (١٬٥٨١ ملفاً، صفر حذف).
- **الدفعة ٢ ✅ (القرار `D‑0022`):** ملف القواعد في `live/` مع وثيقة الربط + الحزمة في
  `history/` + `fetch_archive.py` باقٍ في الجذر بعد مسح إشارات نظيف + سطر تعديل المسار الدستوري.
- **الدفعة ٣ ✅ (القرار `D‑0023`):** الكنز النقي الفريد (٢٠٠ نسخة + شهادة البصمات) في
  `history/clean/` + إغلاق الاستخراج.
- **الدفعة ٤ ✅ (القرار `D‑0024`):** `bot/` مبني من التاج — `bot/NOVA.py` نسخة حرفية
  (بصمة `3e08f1f8f103…` · ٢٦٢ دالة · صفر جروح) + وثيقة النسب؛ النشر على Render ما زال `BACKLOG`.

## لاستئناف العمل (جلسة جديدة)

اقرأ بالترتيب: `CONSTITUTION.md` ثم `docs/CONSTITUTION-AMENDMENTS.md` (خصوصاً حكم الدمج د-١..د-٣)
ثم هذه الوثيقة ثم `docs/REORG-PLAN-2026-09-16.md`. الزم الدستور حرفياً.
