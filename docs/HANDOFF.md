# 🤝 وثيقة التسليم الحية — docs/HANDOFF.md

> **الغرض (الحكم الدستوري د-٣):** أي جلسة جديدة — أو المستخدم من تيرمكس — تستأنف من هنا بلا أي فقدان.
> آخر تحديث: 2026-09-16 ليلاً — الدمج الكبير في main + حماية الأسرار + التوثيق.

## أين نحن الآن
- **إعادة التنظيم كاملة داخل main رسمياً ✅:** الدفعات ١–٤ + دفعة وثائق النتائج + شهادة الجهوزية هبطت بدمج تيرمكس `d126ac7` (فرع `arena/01a0ab14`) ورُفعت. البنية: `history/` (١٥٨١ ملفاً + `clean/` ٢٠٠ نسخة نقية بشهادة بصمات) · `live/` · `bot/NOVA.py` = التاج V7.0.1 النقي (بصمة `3e08f1f8f103…`) · `old/`.
- **حماية الأسرار مثبتة ✅ (`c3bc00b`):** `.env` و`.env.*` ونواتج التنقية محظورة في `.gitignore` — تطابق حرفي لحماية `arena/01a0aa83`. الملف المحلي لم يُمس (مقدس).
- **خزنة تيرمكس:** تعديلات ما قبل الدمج محفوظة في `git stash` — تطابقها البايتي مع `arena/01a0aa83` مثبت بالبصمات — صفر محتوى فريد؛ مصيرها بقرار المستخدم.
- **اعتماد الرفع من تيرمكس:** توكن وصول شخصي مخزّن (`credential.helper store`).

## القواعد الحديدية (مختصرة — التفصيل في الدستور)
1. لا دمج ولا طلب دمج ولا دفع إلى `main` من أي وكيل (د-١/د-٢) — الدمج من تيرمكس: `bash tools/merge_main.sh`
2. لا تحريك: `nova_v8/` · `tools/` · `crypto_archive/` · `data/` · ملفات الدستور في الجذر.
3. لا حذف مكررات إلا بعد إثبات البوت القائد وبأمر صريح.
4. لا ذكاء اصطناعي يقترب من البوت — كود Gemini يبقى معطلاً.
5. البيئة تُعاد تدويرها — كل ثمين يُلتزم فوراً؛ شجرة الفحص تُبنى: `bash new/extract.sh`

## الخطوة التالية
1. 🔴 **تدوير المفاتيح الثلاثة (بيد المستخدم وحده):** `docs/KEY-ROTATION-GUIDE.md` — بينانس ثم تيليجرام ثم جيميناي.
2. **الفروع الصغيرة الثلاثة غير المدموجة** (دمجها حق المستخدم): `arena/01a0aa83` (أداة التنقية + حكم المفاتيح التجريبية — يحتاج حلاً يدوياً بسيطاً لتداخل `.gitignore` و`LOG.md`) · `arena/01a0a9f3` (تسجيل أول دمجة) · `arena/01a0abc8` (كتلة سجل الدماغ). لا يُدمج `arena/01a0a137` ولا `arena/01a09b25` أبداً.
3. **الدفعة ٥ — الحذف:** فقط بعد إثبات البوت القائد وبأمر صريح.
4. **ما بعد التنظيم:** جولة الفريم `L-0007` (تدقيق العقل) · نشر Render ما زال `BACKLOG`.

## لاستئناف العمل (جلسة جديدة)
اقرأ بالترتيب: `CONSTITUTION.md` ثم `docs/CONSTITUTION-AMENDMENTS.md` (خصوصاً د-١..د-٣) ثم هذه الوثيقة ثم `docs/REORG-PLAN-2026-09-16.md`. الزم الدستور حرفياً.
