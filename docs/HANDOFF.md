# 🤝 وثيقة التسليم الحية — docs/HANDOFF.md

> **الغرض (الحكم الدستوري د-٣):** أي جلسة جديدة — أو المستخدم من تيرمكس — تستأنف من هنا بلا أي فقدان.
> آخر تحديث: 2026-09-17 فجراً — جولة الصقل النهائي مكتملة والبنية مجمدة (D‑0033).

## أين نحن الآن
- **البنية الاحترافية كاملة ومجمدة ✅ (D‑0027..D‑0033):** جذر = طبقة الدخول فقط · `tools/` كل الأدوات بلا استثناء · `docs/` الست الدستورية + constitution/designs/lanes/security · `live/` القواعد + الربط + توثيق المنفِّذ · `new/` بوابة استقبال دائمة · `history/` ١٨٣٥ ملفاً لا يُلمس داخلياً · `old/` المتقاعد.
- **بوابة دخول حقيقية ✅:** `README.md` (المهمة + بروتوكول الاستئناف + الخريطة + الحدود الحديدية).
- **فهرس ذاتي التوليد ✅:** `INDEX.md` يُعاد ولادته بأمر `python3 tools/build_index.py` — أرقامه تطابق الموثق (١٩٧٣ ملفاً متعقَّباً).
- **قاعدة اتحاد السجل دائمة ✅:** `.gitattributes` — دمجات `LOG.md` القادمة بلا تعارض.
- **الفروع نظيفة ✅:** على GitHub بقي `main` + الفرعان غير المدموجين فقط (`arena/01a0a137` المحظور دستورياً — لا يُدمج أبداً · `arena/01a09b25`).
- **اعتماد الرفع من تيرمكس:** توكن وصول شخصي مخزّن (`credential.helper store`).

## القواعد الحديدية (مختصرة — التفصيل في الدستور)
1. لا دمج ولا طلب دمج ولا دفع إلى `main` من أي وكيل (د-١/د-٢) — الدمج من تيرمكس بيد المستخدم وحده.
2. لا تحريك: `nova_v8/` · `tools/` · `crypto_archive/` · `data/` · ملفات الدستور في الجذر — **والبنية كلها مجمدة الآن (D‑0033): أي تغيير هيكلي = قرار مستخدم جديد**.
3. لا حذف مكررات إلا بعد إثبات البوت القائد وبأمر صريح (الدفعة ٥ من خطة 2026-09-16).
4. لا ذكاء اصطناعي يقترب من البوت — كود Gemini يبقى معطلاً.
5. البيئة تُعاد تدويرها بين الجولات — كل ثمين يُلتزم فوراً ويرفع.

## الخطوة التالية (بترتيب الأولوية)
1. ~~تدوير المفاتيح~~ — **أُغلق بحكم المستخدم 2026-09-17: كل المفاتيح تجريبية مؤقتة لا تهم — لا تدوير مطلوباً** (D‑0034).
2. **جولة الفريم `L-0007`** (تدقيق العقل): العودة للمهمة العليا — البحث بأرقام محسوبة.
3. **الدفعة ٥ — الحذف:** مشروطة بإثبات البوت القائد وأمر صريح.
4. نشر Render يبقى `BACKLOG`.

## لاستئناف العمل (جلسة جديدة)
اقرأ بالترتيب: `CONSTITUTION.md` ثم `docs/CONSTITUTION-AMENDMENTS.md` (خصوصاً د-١..د-٣) ثم هذه الوثيقة ثم `README.md`. الزم الدستور حرفياً.
