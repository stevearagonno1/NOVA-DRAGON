# DECISIONS — سجل القرارات الملزمة (Append‑only)

تنسيق موحَّد: `D-xxxx | تاريخ | القرار | السبب | الأثر`. لا يُحذف سطر ولا يُعدَّل نصّه؛
الإلغاء يكون بسطر جديد `D-yyyy يُلغي D-xxxx`. كل قرار يجب أن يكون قابلاً للاختبار بأمر أو ملف.

---

**D-0001 | 2026‑09‑12 | كود `nova_v8/**` مُجمَّد أثناء إعداد البيانات والتشغيل.**
السبب: أي تعديل داخل المحرك أثناء جولة بحث يُبطل مقارنة النتائج السابقة بها.
الأثر: معالجة خلل بيانات Binance صُنعت في `tools/rebuild_archive.py` خارج الحزمة. **سارٍ.**

**D-0002 | 2026‑09‑12 | كشف وحدة الطوابع الزمنية لكل ملف (ms حتى 2024‑12، µs من 2025‑01) إلزامي في أي سكربت بيانات.**
السبب: `fetch_archive.py` يفترض `unit="us"` فيُنهال نافذة التدريب إلى 1970 وتفشل عينة التحقق.
الأثر: قاعدة في `AGENTS.md` §2.7 لكل سكربت جديد. **سارٍ.**

**D-0003 | 2026‑09‑13 | استبعاد كاش المصفوفة `*__matrix_*` والبيانات الخام من git (حد 100MB).**
السبب: 137MB للمصفوفة، ~450MB zips — كلها قابلة لإعادة التوليد.
الأثر: `.gitignore` مُفعَّل؛ الرفع = 37→46 ملفاً نظيفاً. **سارٍ.**

**D-0004 | 2026‑09‑13 | نموذج «عقل + مسارات»: محادثة تنسيقية واحدة للعقل، ومحادثة مستقلة لكل تجربة طويلة.**
السبب: تجارب السويپ ساعات؛ مزجها مع التخطيط يُفسد كلاهما، وسياق المحادثة يضيع بلا كتابة.
الأثر: `AGENTS.md` + `docs/BRAIN.md` + `docs/lanes/`؛ العقل لا ينفّذ تجارب. **سارٍ.**

**D-0005 | 2026‑09‑13 | لا تشغيل تجربة قبل اجتياز `tools/canary.py` (بوابة سلامة البيانات).**
السبب: الخلل الوحيد الذي كاد يُبطل جولة 3.5 ساعة كان في البيانات، وكان سيُكشف في دقائق.
الأثر: أداة جديدة + شرط في قالب المسار. مُجرَّبة: PASS في 10 ثوانٍ على بيئة مختلفة. **سارٍ.**

**D-0006 | 2026‑09‑13 | التسامح الرقمي المعتمد: `|Δnet| ≤ 1e-9$` ومطابقة `trades` تامّة. ممنوع الادعاء بمطابقة بتّية.**
السبب: القياس أظهر `net=-98.5903161993732` (pandas 3.0.5/numpy 2.4.6) مقابل
`-98.59031619937323` (pandas 2.2.3/numpy 2.3.5): فرق خانة في آخر رقم، ومنطق مطابق.
الأثر: صياغة التوثيق تتغيّر من «مطابقة تامة» إلى «مطابقة ضمن ±1 ULP»، وعبارة
«476 تركيبة مطابقة بايت‑ببايت» صُحِّحت إلى «446 نصية + 30 ضمن ULP». **سارٍ.**

**D-0007 | 2026‑09‑13 | لا تُنشر ولا تُروَّج أي تركيبة حياً قبل عبور «بوابة القبول» (BRAIN.md §5).**
السبب: 8/8 نهائيين سلبيون خارج العينة في `adaptive_trend` و`donchian`. أي تلميح للتشغيل الآن = خسارة مال.
الأثر: الحالة الرسمية: **NO‑GO** على كل التركيبات الموثقة؛ لا مسار تنفيذ حي. **سارٍ.**

**D-0008 | 2026‑09‑13 | نتائج `sweep_results/donchian/` معزولة: لا تُقارن بـ `adaptive_trend` ولا تُعَدّ مُشتقَّة من الكود المرفوع.**
السبب: ملفاتها تُعوِّم `NOVA_DON_MACRO/NOVA_DON_CHOP` وهذان المفتاحان **غير موجودين** في `nova_v8/`
(`grep` = 0 مراجع)؛ شبكة donchian في المحرك = `5×4×2 = 40` لا 80.
الأثر: L‑0001 يُعيد donchian بمحاور المحرك الفعلية. **سارٍ حتى يُغلِق L‑0001.**

**D-0009 | 2026‑09‑13 | أي شبكة سويپ جديدة تُفحص للأعمدة الخاملة قبل التشغيل (قاعدة `A8/A9` في `tools/audit_lane.py`).**
السبب: من 1920 تركيبة، 469 نتيجة net مميزة فقط (24%). `V3B` خامل في 960/960 زوجاً، `V3C` في 480/960،
وفي donchian: `DON_MACRO` و`DON_CHOP` خاملان في 40/40 زوجاً ⇒ ~4× حوسبة مهدورة ومبالغة منهجية في «حجم البحث».
الأثر: فحص ما قبل التشغيل إلزامي في قالب المسار؛ L‑0002 يورّد الشبكة المخفَّضة. **سارٍ.**

**D-0010 | 2026‑09‑13 | إصدارات البيئة المثبَّتة صارت جزءاً من العقد: `requirements.txt` في جذر المستودع.**
السبب: المرجع مُنتَج بـ pandas 2.2.3/numpy 2.3.5/py 3.13؛ مسارات تعمل بإصدارات أحدث تُنتج فروق ULP وتضيّع وقت التدقيق.
الأثر: المسارات تلتزم `pip install -r requirements.txt` (أو تسجيل الانحراف صراحة في HANDOFF). **سارٍ.**

**D-0011 | 2026‑09‑13 | قراءة أي CSV من `sweep_results/**` بـ `encoding="utf-8-sig"` حصراً.**
السبب: المحرك يكتب BOM؛ القراءة بدونه تجعل أول عمود `\ufeffNOVA_AT_TRAIL` فيختفي من الفحص.
الأثر: خطأ تدقيقي زائف («384 تركيبة فريدة بدل 1920») ظهر ثم شُخِّص؛ القاعدة في `AGENTS.md` §2.6. **سارٍ.**

**D-0012 | 2026‑09‑14 | إصلاح عطلين في `adaptive_trend` بأقل جراحة، مع مفتاح تراجع لكل واحد.**
السبب: قياس `sweep_train.csv` أثبت أن V3A ترفض كل صفقة (960 تركيبة بـ`net=0.00`) لأن شرطها يتناقض
رياضياً مع شرط الدخول، وأن V3B يُقاس على `ATR-4h` مقابل جسم شمعة 5m فلم يشتغل ولا مرة (960/960).
الأثر: `NOVA_AT_V3A_LEGACY=1` و`NOVA_AT_V3B_ATR=4h` يعيدان السلوك القديم حرفياً. مُثبَت: الأساس بقي
‏+932.93$/17 صفقة، والكاناري PASS. وأول حكم بعد الإصلاح: فرضية (ب) تضر (‎-432.52$). **سارٍ.**

**D-0013 | 2026‑09‑14 | كل تجربة تكتب `env_dump.txt` بجانب نتائجها (قيم كل `NOVA_*` + حالة الفلاتر + مسار البيانات).**
السبب: الأرشيف كله موسوم `experiment=baseline`، فلم يمكن الحسم هل كان `NOVA_DON_FILTER` مضاءً في
‏`test_don_filter` أم لا (مجلدان نتيجتهما واحدة بالحرف: ‎-2,108.11$/404 صفقة ⇒ لا زوج مقارنة لدعوى «الفلتر خفّض الألم 98٪»).
الأثر: نتيجة بلا `env_dump.txt` = غير مؤهلة للاعتماد. مُضاف لقالب المسار. **سارٍ.**

**D-0014 | 2026‑09‑14 | بيانات 2021‑09→2026‑08 محروقة: لا يُقبل منها «إثبات» جديد، والإثبات المستقبلي = الأمام.**
السبب: استُخدمت في الضبط وفي **اختيار العملات** معاً؛ الاختبار الحقيقي الوحيد المتبقي هو ما لم يحدث بعد.
الأثر: مرحلة Testnet تصير هي البوابة، وتُعلَّق جولات السويپ التجميلية على نفس البيانات. **سارٍ.**

**D-0015 | 2026‑09‑14 | `adaptive_trend` تُدفن موثَّقة؛ والسلاحف تبقى «جناح صعود» فقط.**
السبب: على النافذة النظيفة الأخيرة (2021‑09→2023‑08): التركيبة التي بدت اكتشافاً (+142.48$ على الاختبار)
انقلبت إلى **‎-495.35$**، والفائز الرسمي ‎-143.86$. والسلاحف: ‏+3,126$ في الصعود مقابل ‎-4,072$ على كل العملات في `sf/`.
الأثر: لا بوست رابع في المحفظة؛ ومن قائمة الإيرادات المتوقعة يُشطب «+5,104$» ويُثبت «+3,126$ في الصعود فقط». **سارٍ.**

---

## قرارات معلَّقة (بانتظار قرار من المستخدم أو نتيجة مسار)

- **P‑A** (2026‑09‑14) هل يُصلَح `fetch_archive.py` في المصدر (يخالف D‑0001 مؤقتاً) أم يبقى العيب ونعتمد `tools/rebuild_archive.py`؟
  توصية العقل: **يُصلَح** لأنه لا يغيّر منطق الاستراتيجيات، ويحمي كل مشغّل مستقبلي (خاصة من يقرأ السجل لا الأدوات).
- ~~P‑B~~ **محموم**: الأرشيف يثبت أن MACRO/CHOP كانا ميتين؛ لا يُضافان. بديلهما الحقيقي `NOVA_DON_FILTER` يحتاج زوج A/B مسجَّل الإعداد (D‑0013) — L0005.
- ~~P‑C~~ **محموم (14 سبتمبر):** رفع المستخدم العملات فعلاً بدفعات الجوال 1‑4 (14 عملة على main — `docs/DATA-INVENTORY-2026-09-14.md`) فانفتح اختبار المتانة عبر العملات مباشرة.

**D-0016 | 2026‑09‑14 | عقد الإصدارات (D‑0010) مستقرّ في مساره الجديد `docs/source/requirements.txt` — كل أمر تثبيت يستدعيه من هناك.**
السبب: نقلُ متفرقات جذر المستودع إلى `docs/source` (تنظيم 2026‑09‑14) ترك الإحالة القديمة «جذر المستودع» معلّقة على ملف غير موجود هناك، وأي وكيل ينفّذ `pip install -r requirements.txt` من الجذر سيفشل.
الأثر: `INDEX.md` والوثائق اللاحقة تُحيل للمسار الجديد؛ محتوى العقد لم يتغير (pandas 2.2.3 / numpy 2.3.5 / pyarrow 25.0.1)، وقد اجتازه الكاناري اليوم من مساره الجديد. **سارٍ.**

**D-0017 | 2026‑09‑14 | أعداد الصفقات في جدول §1 من `docs/ARCHIVE-VERIFIED-2026-09-14.md` غير معتمدة حتى تصحيحها؛ المرجع الوحيد للأعداد = إعادة الحساب من `research_results.csv`.**
السبب: تدقيق المرآة أعاد الحساب فوجد الموثّق (A=3,356 / B=3,115 / C=2,136) لا يطابق الملفات (A=3,457 / B=2,743 / C=2,028)، وصافي B = ‎+3,126.56$ لا ‎+3,126.54$؛ أما صافيا A وC فطابقا حرفياً وحكم الدستور (−18,378/+3,127/−11,375) صحيح على دقته.
الأثر: الأوامر كلها في `docs/STATS-2026-09-14.md` §7؛ تصحيح الجدول نفسه يحتاج إذن تعديل صريحاً من المستخدم. **سارٍ.**

- تحديث **P‑A** (2026‑09‑14، تدقيق المرآة): الأساس الواقعي تغيّر — ملف الجذر `fetch_archive.py` الحيّ لا يحتوي افتراض `unit="us"` أصلاً (بقي في `archive/code_snapshot/nova_v8/fetch_archive.py:49` فقط). التوصية الجديدة: **يُقفل P‑A بلا إصلاح إضافي**.

**D-0018 | 2026‑09‑14 | إقفال P‑A: لا إصلاح إضافي في `fetch_archive.py` — المصدر الحيّ سليم.**
السبب: تدقيق المرآة أثبت أن افتراض `unit="us"` غير موجود في ملف الجذر أصلاً (يكتب `open_time` عمود int64 بالمللي ثانية)؛ بقي في `archive/code_snapshot/nova_v8/fetch_archive.py:49` فقط — أرشيف مجمد لا يُشغَّل.
الأثر: تُعتمد `tools/rebuild_archive.py` للبيانات القديمة عند الحاجة، ويُقفل P‑A نهائياً. **سارٍ.**

**D-0019 | 2026‑09‑14 | رفع عزل نتائج donchian القديمة (تعديل D‑0008) + شبكة donchian المعتمدة = 20 تركيبة.**
السبب: سويب L‑0001 بمحاور المحرك الفعلية أعاد إنتاج ملفات `sweep_results/donchian`: 20/20 تركيبة (ENTRY×EXIT) مطابقة على التدريب ضمن التسامح الرقمي (5 نصية + 15 بفرق ≤ 2e‑12$؛ الصفقات والفوز متطابقان حرفياً)، ونهائيا الاختبار المتقاطعان ضمن ULP — فالمحوران الغائبان DON_MACRO/DON_CHOP كانا بلا أثر أصلاً.
الأثر: (1) نتائج donchian القديمة صالحة للمقارنة مع adaptive_trend؛ (2) محور NOVA_DON_FILTER خامل قياساً 20/20 ⇒ كل سويب donchian قادم = ENTRY1(5)×EXIT1(4) = 20 تركيبة؛ (3) دعوى «الفلتر خفّض الألم 98%» تبقى غير قابلة للقياس حتى إصلاح برمجي بكلمة «أصلح» من المستخدم (L‑0005 موقوفة عليه). **سارٍ.**


**D‑0020 | 2026‑09‑14 | حل تصادم ترقيم المسارات بعد توحيد main (L‑0006) + قفل P‑C.**
السبب: أثناء «ادمج» ظهر أن الرقم 0004 استُعمل بثلاثة معانٍ في جهات متوازية (L‑0004 = تدقيق الأرشيف في لوحة المسارات · «L‑0004» في طابور BRAIN وسؤال P‑C بمعنى رفع العملات · L0004 عند الجلسة الموازية بمعنى مختبر الفرضيات) لأن كل جلسة رقّمت على حدة قبل التوحيد.
الأثر: (1) مسار مختبر الفرضيات = **L‑0006** (الملف `docs/lanes/L0006-hyp-lab-first-sweep.md`؛ المخرجات بقيت في `research/hyp_lab_out/L0004/` بترقيم جلستها الأصلية)؛ (2) الجولة القادمة على جهاز المستخدم — إعادة المقاسات الخمس + F‑197 على أرشيف 5 سنوات — = **L‑0007**؛ (3) P‑C محموم (الرفع حدث فعلاً)؛ (4) أي مسار جديد يُرقَّم من L‑0007 صعوداً بعد الاطلاع على لوحة المسارات. **سارٍ.**

**D‑0021 | 2026‑09‑16 | بوابة إشارات الدفعة ١: تحديث المسارات الحية في ثلاث أدوات قبل النقل إلى history/ + الوثائق التاريخية لا تُمس.**
السبب: مسح الإشارات قبل الدفعة ١ (أمر الخطة §٢-٦) وجد ثلاث أدوات تقرأ المسارات المنقولة وقت التشغيل: `tools/organize_incoming.py` (وجهات `archive/` و`bot_versions` و`hypotheses` و`code_drafts` + `research/nova_v8_out`) · `tools/archive_report.py` (`research/nova_v8_out`) · `tools/audit_lane.py` (الافتراضي `sweep_results/`). ووجد عشرات الإشارات في `docs/` كلها في سجلات تاريخية (الحارات/التدقيقات/الإحصاءات/السجلات) — ذاكرة ثابتة لا يصح تعديلها (البند ٢٢) ولا أثر لها على أي تشغيل.
الأثر: حُدّثت مسارات الأدوات الثلاث وفق البند ١١ (إصلاح الأدوات داخل مجلد الأدوات فقط، بسطر قرار موثق) · الوثائق التاريخية بقيت حرفياً كما هي بوصفها واقعاً تاريخياً · `INDEX.md` حُدّث إلى الخريطة الجديدة في نهاية الدفعة. **سارٍ.**

**D‑0022 | 2026‑09‑16 | الدفعة ٢ منفَّذة: ملف القواعد الحية إلى live/ + الحزمة إلى history/ + fetch_archive.py يبقى في الجذر بعد مسح الإشارات.**
السبب: نقاط القرار ١–٤ محسومة بتوصيات الخطة بموافقة المستخدم. مسح إشارات `fetch_archive.py` (استحقاق الدفعة ٢ وفق الخطة §٣-أ) وجد صفراً من الإشارات الحية في الكود — كل الإشارات في سجلات تاريخية (BRAIN · DECISIONS · EXECUTION_REPORT · HALAL-LIQUID-20) + سطر فهرس وصفي.
الأثر: (1) `docs/LIVE-TRADING-RULES.md` → `live/LIVE-TRADING-RULES.md` مع وثيقة ربط `live/README.md` (المنفِّذ F‑197 ممنوع التحريك ومؤشَّر إليه) + سطر تعديل المسار في سجل التعديلات الدستورية · (2) `nova_upload_bundle.tar.gz` → `history/` (البصمة معتمدة في سجل الإحصاء `new/CENSUS.md`) · (3) `fetch_archive.py` يبقى في الجذر · (4) نقطة القرار ٣ (وثائق النتائج في `docs/`) مؤجلة إلى دفعة لاحقة كما اعتمدت التوصية. **سارٍ.**

**D‑0023 | 2026‑09‑16 | الدفعة ٣ منفَّذة: الكنز النقي الفريد إلى history/clean/ + المكررات إلى old/bundle_rest/ (غير متعقبة بالتصميم) + إغلاق staging.**
السبب: بعد تحديث مسار الحزمة في `new/extract.sh` إلى `history/` (أثر `D‑0022`)، أُعيد بناء الشجرة بأمر واحد: البصمة مطابقة، ٢١+١ كيساً فُكّت، ٤ أسماء عربية صُححت، الإحصاء والعزل تجدد (١٧ ملف مفاتيح عاد إلى `new/_quarantine/`). بوابة الحتمية: السجلان المعاد توليدهما يطابقان المعتمد (٢٥٩ من ٢٦٠ سطراً حرفياً؛ السطر الـ٢٦٠ = `extract.sh` نفسه — تغير ذاتي متوقع بعد تحديث مساره، اعتُمد أحدثُ توليد بوصفه أصدقَ سجل للواقع). ثم الانتقاء: نسخة واحدة لكل بصمة `sha256` عبر ٣٤٩ ملفاً (الممثل = أقصر مسار ثم الأول أبجدياً) ⇒ **٢٠٠ نسخة فريدة (٢٥.٩ ميغابايت)** في `history/clean/` مع شهادة البصمة الكاملة `MANIFEST-CLEAN.md`؛ المطابقة مع الإحصاء = صفر مفقود من بصمات الكنز الـ٢٥٨؛ التاج V7.0.1 في مكانه المعلوم بالنقي (٣٨٣٬٣٧٤ بايت · `3e08f1f8f103`) — منه تُبنى `bot/NOVA.py` في الدفعة ٤.
الأثر: (1) الـ١٤٩ مكرراً في `old/bundle_rest/` على القرص **غير متعقبة بالتصميم** — قابلة للاشتقاق ١٠٠٪ من الحزمة المتعقبة (`bash new/extract.sh`) وفق بروتوكول الاستمرارية §٥-ب/٧-ب والبند الدستوري ١٩-٢، بحماية `.gitignore` ووثيقة `README.md` متعقبة · (2) أُغلق staging `new/`: لا يبقى فيه إلا المتعقَّب (الإحصاء والفحوص والأداتان) + المعزل · (3) صفر حذف في كل الدفعة. **سارٍ.**

**D‑0024 | 2026‑09‑16 | الدفعة ٤ منفَّذة: بناء bot/ من التاج — نسخة حرفية بلا أي تعديل منطقي.**
السبب: بأمر المستخدم الصريح المنفصل (شرط الخطة §٣-هـ). نُسخ التاج `history/clean/NOVA_bin_pending/جميع اصدارات البوت/1/NOVA.Finally_X/NOVA.py` حرفياً إلى `bot/NOVA.py` (نسخ لا نقل — الأصل الأرشيفي بقي في النقي وشهادته صحيحة)، والتحقق الكامل: تطابق بايت-ببايت (`cmp`) + بصمة `3e08f1f8f103d391207226d5973add13d4d91c52ad8f7911f5f221de39e79234` + `ast` سليم + 262 دالة + 7,230 سطراً + صفر جروح — كلها مطابقة لإثبات `new/CROWN-CHECK.md`.
الأثر: (1) `bot/` قائم بملف دخول ثابت `bot/NOVA.py` لربط Render لاحقاً (النشر يبقى `BACKLOG`) · (2) `bot/README.md` يوثق النسب والبصمة ووضع Gemini المعطَّل وقواعد التعديل · (3) كود الذكاء الاصطناعي في البوت يبقى معطلاً بلا مفتاح بيئة (حكم المستخدم) · (4) لا تعديل على `bot/` مستقبلاً إلا بسطر قرار. **سارٍ.**
