# BRAIN — ميثاق «العقل» (المحادثة التنسيقية العليا)

الملف الشامل لمشروع NOVA_V8. صُمّم ليُقرأ standalone: أي محادثة جديدة تصير عقلاً
بقراءته + `CONSTITUTION.md` + `docs/DECISIONS.md` + `docs/lanes/INDEX.md`.

---

## 1) الذاكرة: المستودع هو العقل

المحادثة تُنسى، والقرص المؤقت يُحذف. لذلك:

| الطبقة | العمر | ماذا يحمل |
|---|---|---|
| `history/docs/PROJECT_LOG.txt` | أبدي | الرواية: ماذا حدث، ولماذا، وأي خلل عُولج |
| `docs/DECISIONS.md` | أبدي (Append‑only) | القرارات الملزمة وأثرها — الذاكرة التنفيذية |
| `docs/BRAIN.md` (هذا الملف) | يُحدَّث | الحالة الآن + السياسة + بوابة القبول |
| `docs/lanes/*` | لكل تجربة | العقد، التنفيذ، HANDOFF، حكم العقل |
| المحادثة | ساعات | غرفة اجتماعات: تقرأ وتكتب في الأعلى ولا تحل محلهما |

**قاعدة ذهبية:** بعد كل قرار أو نتيجة — commit + push. إن لم يُدفَن في git فهو لم يحدث.

## 2) مهمات العقل (ما وظيفته حصراً)

1. **تحويل الأفكار إلى مسارات** مغلقة قابلة للإيقاف: هدف واحد + عقد تنفيذ + ميزانية زمنية + شروط توقف.
2. **التدقيق المستقل:** لا يصدّق عقلٌ رقماً من مسار حتى يعيد اشتقاقه من ملفات المخرجات (`tools/audit_lane.py`).
   التدقيق واجب حتى على النتائج التي تُرضي المستخدم.
3. **حفظ الاتساق:** رفض تجربة ثانية على نفس التركيبة، ومنع تعديل `nova_v8/**` بلا قرار، ومطابقة
   التوثيق مع الواقع (إذا قال السجل «مطابقة تامة» والقياس «±1 ULP» → السجل هو المُصحَّح).
4. **إدارة الأولويات والمخاطرة:** ما الذي يستحق ساعات حوسبة أصلاً، وما الذي يجب أن يُغلق بلا تشغيل.
5. **بوابة القرار:** لا تركيبة تنتقل من «أجمل نتيجة سويب» إلى «مرشّح للتشغيل» إلا بعبور §5.
6. **الترجمة إلى فعل:** إعطاء المستخدم الأمر/الملف/السطر التالي في تيرمكس، جاهزاً للنسخ.
7. **المراجعة الدورية:** بعد كل إغلاق مسار، تحديث §3 و§4 و`INDEX.md` وسطر في `history/docs/PROJECT_LOG.txt`.

## 3) الحالة الآن — مُدقَّقة من المستودع (2026‑09‑13، وأُعيد تدقيقها 2026‑09‑14)

الأدلة: `8f47e61` (موجود على GitHub — تحقق `gh api repos/stevearagonno1/NOVA-DRAGON/commits/8f47e61`؛ الاستنساخ المحلي ضحل فلا يراه git المحلي) + إخراج `tools/audit_lane.py` و`tools/canary.py` + قراءة `crypto_archive/BTCUSDT_1m.parquet` مباشرة (pyarrow).

> **تدقيق المرآة 2026‑09‑14:** كل رقم في هذا القسم أُعيد اشتقاقه من الملفات؛ الجدول الكامل والأوامر في `history/docs/STATS-2026-09-14.md`، والكاناري أُعيد إنتاجه **PASS** في بيئة ثالثة (|Δ|=2.842e‑14).

### ما هو صحيح ومؤكد
- جولة `adaptive_trend` اكتملت: **1920 تركيبة، فريدة بالكامل، صفر فشل، صفر نقص**.
  تغطية الشبكة = `5×4×4×3×2×2×2` تماماً (A1 PASS).
- عينة التحقق تُنتَج حرفياً في بيئة مختلفة: `net=-98.5903161993732 trades=68`
  مقابل مرجع `-98.59031619937323` (|Δ|=2.8e‑14) ⇒ **الحاسوب والمنطق سليم** (canary: PASS).
- 734 تركيبة موجبة على التدريب (أعلاها +935.10$) مقابل **8/8 سالبون على الاختبار**
  (-171.16$ → -161.47$) ⇒ حارس فرط الملاءمة عمل.
- `donchian` أيضاً: 8/8 نهائي سالبون على الاختبار (-393.17$ → -323.68$).
- المستودع يُقرأ من GitHub (تحقق 2026‑09‑14 عبر `gh api`؛ فرع main يطابق شجرتنا). الدفع موثق من 2026‑09‑13 ولم يُعَد اختباره (لا رفع بلا إذن).
- البيانات في git: `crypto_archive/BTCUSDT_1m.parquet` = 89,135,771 بايت (~85 MiB؛ `du` يعرضها 86M) + `data/` سبعة ملفات ≈ 18.7MB. «الخدمة عبر `raw`»: **غير مقاسة** من بيئة التدقيق 2026‑09‑14 (raw.githubusercontent.com محجوب منها)؛ الملفات متتبعة على main ⇒ أي وكيل يعمل بلا تنزيل.

### تصحيحات أضافها التدقيق (اقرأ `history/docs/AUDIT-2026-09-13.md` و`history/docs/ARCHIVE-VERIFIED-2026-09-14.md`)
| ما كان موثقاً | ما هو صحيح فعلاً | الأثر |
|---|---|---|
| «الـ476 مطابقة بايت‑ببايت» | 446/476 نصية؛ 30 اختلفت في آخر رقم (`|Δnet|≤2.8e‑14`) | لا أثر على الاستنتاج؛ الأرقام صحيحة ضمن ULP |
| «1920 تركيبة = بحث واسع» | **469 نتيجة net مميزة فقط** (24%)؛ `V3B` خامل 100%، `V3C` خامل 50% | 3 من 4 أضعاف الحوسبة كانت تكراراً ⇒ الحيز الفعلي للبحث أصغر بكثير |
| «الفائز الرسمي: V3B=1» | V3B خامل تماماً، وأفضل net على الاختبار **مشترك بين 2 نهائي** (تعادل دقيق) | التركيبة «الفائزة» ليست فريدة ولا V3B سبباً |
| «donchian من نفس المحرك» | ملفاته تستخدم `DON_MACRO/DON_CHOP` و**لا وجود لهما في `nova_v8/`** (الصفر مراجع) | نتيجة donchian **غير قابلة لإعادة الاشتقاق** من الكود المرفوع ⇒ لا تُقارن بـ AT |

### الفجوات القائمة (الحقيقية)
- **لا تركيبة مجتازة للبوابة** ⇒ صفر تشغيل حي (عدا دوران الصعود للسلاحف، وهو مؤهل للتجربة على Testnet لا للربح الموعود).
- **تصحيح من تدقيق 14 سبتمبر:** كنت كتبت «لا شيء صالح للنشر» بينما الدليل كان يغطي استراتيجيتين من **ثماني** مسجلة؛
  الآن نطاق الحكم مبني على أرشيف 72 تجربة (`history/docs/ARCHIVE-VERIFIED-2026-09-14.md`) لا على ملفين.
- بيانات 11 عملة أخرى غير مرفوعة (موجودة على الجوال): أحجامها المنفردة **غير مقاسة من المستودع** — المنقول الوحيد سجلُّ الجوال «623MB لـ11 عملة» في `LOG.md`، ولا يمكن التحقق منه من هنا ⇒ بدونها لا اختبار متانة عبر العملات.
- ~~المحرك لا يقرأ `DON_MACRO/DON_CHOP`~~ **حُسم 2026‑09‑14 (L‑0001/D‑0019):** المحوران كانا بلا أثر؛ المحرك الحالي يعيد إنتاج نتائج donchian القديمة (20/20 ضمن التسامح) ⇒ العزل رُفع، وتجربة المحورين مُلغاة نهائياً، وشبكة donchian القادمة = 20 تركيبة (5×4).
- ~~`fetch_archive.py` الأصلي ما زال يفترض `unit="us"`~~ **تصحيح 2026‑09‑14:** الافتراض لم يبقَ إلا في النسخة المجمدة `archive/code_snapshot/nova_v8/fetch_archive.py:49`؛ الملف الحيّ `tools/fetch_archive.py` يكتب `open_time` كعمود `int64` بالملي ثانية بلا أي افتراض وحدة (grep: صفر) ⇒ المصدر الحيّ سليم، وP‑A ينتظر الإغلاق الصياغي فقط.

### تدقيق المرآة 2026‑09‑14 (ملخص — التفصيل والأوامر في `history/docs/STATS-2026-09-14.md`)
- **طابق حرفياً:** 1,920 تركيبة فريدة وتغطية 5×4×4×3×2×2×2 · 734 موجبة وأعلاها +935.10$ · 469 net مميزة (24.4%) · V3B خامل 960/960 وV3C 480/960 وV3A فعّال 0/960 · adaptive على الاختبار 8/8 سالب (−171.16 → −161.47) بتعادل القمة بين نهائيين · donchian 8/8 سالب (−393.17 → −323.68) بقمة مشتركة بين 4 · 446/476 نصية و30 ضمن ULP · صفر مراجع DON_MACRO/DON_CHOP · 874 ملفاً/72 تجربة · ثماني استراتيجيات في السجل (سبع لها صفقات في الأرشيف؛ btc_leadlag بلا صفقات) · A/B/C = −18,378.08 / +3,126.56 / −11,375.19 تطابق الدستور · طبقات الانزلاق (0.03/0.10/0.30)% · الكوميت `8f47e61` موجود.
- **الكاناري أُعيد إنتاجه في بيئة ثالثة:** net=−98.5903161993732 · صفقات=68 · |Δ|=2.842e‑14 ⇒ **PASS**.
- **صُحّح في هذا الملف:** وحدة `fetch_archive.py` (الخلل لم يبقَ إلا في السنابشوت المجمد) · «86MB» فُصّلت بالبايت · أحجام عملات الجوال → غير مقاسة · «الخدمة عبر raw» → غير مقاسة من بيئة التدقيق.
- **مسجَّل بلا تعديل (يحتاج إذن المستخدم):** أعداد الصفقات في جدول §1 من `ARCHIVE‑VERIFIED‑2026‑09‑14` (3,356/3,115/2,136) لا تُشتق من الملفات — الفعلي **3,457/2,743/2,028**، وصافي B **+3,126.56** لا +3,126.54 (قُيّد D‑0017). صوافي A وC وحكم الدستور صحيحان.

## 4) ملكية الملفات (مبدأ عدم التداخل)

| الملف | المالك | من يكتب فيه |
|---|---|---|
| `nova_v8/**` | **العقل** | لا أحد بلا `D-xxxx` مُنفَّذ |
| `tools/**` | العقل | أي مسار يقترح أداة؛ العقل يقرّر دمجها |
| `sweep_results/**` | المسار الذي أنتجها | إضافة ملفات المسار فقط، لا تعديل القديم |
| `docs/lanes/Lxxxx-*` | المسار في أقسامه `BRIEF/RUN/HANDOFF`؛ العقل في قسم `VERDICT` | الفصل بالوسوم `<!-- BRAIN-ONLY -->` |
| `docs/DECISIONS.md`, `history/docs/PROJECT_LOG.txt`, `BRAIN.md`, `INDEX.md` | **العقل** حصراً | — |

## 5) بوابة القبول (Adoption Gate) — شرط لازم وكافٍ للانتقال من «بحث» إلى «مرشّح حي»

تُطبَّق على تركيبة واحدة. كلها لازم:
1. **Out‑of‑sample موجب:** `net > 0` على نافذة لم تُستخدم في الفرز ولا في الهضبة (نافذة ثالثة مقفلة).
2. **حجم عينة كافٍ:** `trades ≥ 30` على كل نافذة اختبار، وإلّا فالنتيجة ضجيج.
3. **ثبات الهضبة:** `plateau ≥ 0.6 × net` (الجيران لا يتركون الجبل).
4. **قابلية النقل:** الإشارة نفسها على ≥ 3 عملات من السلة، وبنفس علامة `net`.
5. **تحمل كلفة التنفيذ:** يبقى موجباً عند `SLIPPAGE_TIERS` الثلاث (0.03%/0.10%/0.30%).
6. **قابلية إعادة الاشتقاق:** `audit_lane.py` بلا FAIL، والكاناري PASS، وكل رقم مذكور مع أمر إنتاجه.
7. **حارس العبثية:** إذا كان المحور الذي «يحسّن» النتيجة خاملًا في ≥50% من الأزواج ⇒ لا يُنسَّب سبباً.

> الحالة الرسمية: **NO‑GO** على كل تركيبة **قِيست**. وغير المقاسة تُسمّى «غير مقاسة» — لا «فاشلة».
> بعد تدقيق أرشيف 874 ملفاً (14 سبتمبر): `adaptive_trend` مدفونة (D‑0015)، والسلاحف معتمدة
> **كجناح صعود فقط**، ومقياس «هل تعبر البوابة» صار متاحاً على `research/nova_v8_out/` كله.

## 6) دورة حياة المسار

```
فكرة ─▶ [BRIEF] يكتبها العقل (هدف + عقد + ميزانية + شروط توقف)
      ─▶ تُفتح محادثة جديدة ← تلصق BRIEF حرفياً ← تنفّذ لساعات
      ─▶ [HANDOFF] يملؤها المسار: ملفات + نتائج + ملاحظات صريحة عن الإخفاق
      ─▶ العقل: audit_lane.py + إعادة اشتقاق عشوائية ─▶ [VERDICT]
      ─▶ قرار: ✅ مغلقة-مقبولة / 🔁 مسار تالٍ / ⛔ مغلقة-مرفوضة + سطر في DECISIONS
```

قواعد تشغيل المسارات (لا تُناقَش):
- **ميزانية:** لا تجربة تخمينية > 4 ساعات حوسبة. > ذلك يحتاج تقسيمًا إلى مراحل بحدّ فاصل.
- **لا بدء قبل الكاناري:** `tools/canary.py` أولاً دائماً (يكشف خلل وحدات الزمن خلال دقائق بدل 3 ساعات).
- **resume آمن:** لا تحذف `sweep_train_partial.csv`؛ لا تغيّر تعريف الشبكة في نصف الجولة (يُفسد `done`).
- **الإبلاغ عن السالب إلزامي:** «لم أجد» نتيجة صحيحة ومقبولة. يُمنوع تدوير نافذة الاختبار حتى تصير موجبة.
- **قرص المحرك:** `python3 tools/audit_lane.py <المسار> --json` يُنسخ ملخصه في `HANDOFF` — بلا تدقيق لا قرار.

## 7) جدول أعمال العقل (مفتوح — بالترتيب)

| # | ما | لماذا الآن |
|---|---|---|
| 0 | ✅ **مُنجز 14 سبتمبر**: أرشيف 874 ملفاً رُفع ودُقّق؛ أرقام A/B/C طابقت دستورك حرفياً؛ `adaptive` دُفنت بالدليل؛ V3A/V3B أُصلحا (D‑0012) |
| 1 | إغلاق L‑0000 بحكم رسمي + تصحيح نص `history/docs/PROJECT_LOG.txt` بالأرقام المدقَّقة | التوثيق الحالي يبالغ في 4 مواضع؛ التصحيح رخيص ومسؤول |
| 2 | L‑0002: **تجريد الشبكة من الأعمدة الخاملة** قبل أي سويپ جديد | يوفّر ~4× حوسبة على نفس التغطية، ويُصلح خطأ «1920» المنهجي |
| 3 | ✅ **أُنجز 14 سبتمبر (L‑0001):** donchian أُعيد بمحاور المحرك الفعلية — التدريب يعيد إنتاج القديم 20/20؛ الاختبار 8/8 سالب والنافذة النظيفة 20/20 سالبة؛ محور DON_FILTER خامل 20/20 (D‑0019) | المقارنة بين المسارين صارت صالحة؛ الحكم NO‑GO للنشر |
| 4 | L‑0003: نافذة ثالثة مقفلة + بوابة القبول على 1920 تركيبة (تحليل ثانوي على ملفات موجودة، بلا حوسبة ثقيلة) | السؤال الوحيد الباقي: هل توجد تركيبة تعبر البوابة أصلاً؟ |
| 5 | ✅ **أُنجز 14 سبتمبر (دفعات الجوال 1‑4):** 14 عملة على main — الثمانية المعتمدة كاملة + 6 بحثية (الجرد: `history/docs/DATA-INVENTORY-2026-09-14.md`) | اختبار المتانة عبر العملات (شرط البوابة 4) صار ممكناً — بدأته الجلسة الموازية بمختبر الفرضيات (L‑0006) وتليها L‑0007 على الجهاز |
| 6 | قرار معلَّق: `fetch_archive.py` (P‑A) — يُغلق بلا إصلاح إضافي | تصحيح 2026‑09‑14: المصدر الحيّ (`tools/`) لا يحتوي الافتراض أصلاً؛ بقي في السنابشوت المجمد فقط |
