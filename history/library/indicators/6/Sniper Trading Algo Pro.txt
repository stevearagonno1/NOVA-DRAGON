// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © Kosaiaung0

//@version=6
indicator(' Sniper Trading Algo Pro [V.1.2]', overlay = true, max_labels_count = 500, max_lines_count = 500, max_boxes_count = 500, max_bars_back = 5000)

// Inputs
gr_Basic_Settings = '⭐Basic Settings⭐'
Strategy_Select = input.string('Confirmation', title = 'Strategy Selector', options = ['None', 'Confirmation', 'RE Beta', 'MS Beta', 'SRT Beta'], group = gr_Basic_Settings, tooltip = '-None: Chart ပေါ်တွင် မည်သည့် Strategy မှ ပြသမည်မဟုတ်ပါ။ \n-Lune - Confirmation: အရောင်းအဝယ်နှင့် Trend အတက်အကျကို အတည်ပြုရန် အသုံးပြုသည်။\n-RE Beta: Day trade နှင့် Scalping အတွက် အဓိကသုံးသော ရိုးရှင်းသည့် Strategy ဖြစ်သည်။\n-MS Beta: Momentum ကိုအခြေခံပြီး Day trade နှင့် Scalping အတွက် အဓိကသုံးသည်။\n-SRT Beta: Swing trade များအတွက် အဆင့်မြင့် Strategy ဖြစ်သည်။')
Sensitivity = input.int(4, title = 'Strategy Sensivity', group = gr_Basic_Settings, tooltip = 'တန်ဖိုးမြင့်လေ ဈေးနှုန်းအပြောင်းအလဲအပေါ် တုံ့ပြန်မှုပိုမြန်လေဖြစ်သည် (1m/3m/5m ကဲ့သို့ အချိန်တို Frame များအတွက် သင့်တော်သည်)၊ တန်ဖိုးနိမ့်ပါက မလိုအပ်သော အတက်အကျ (Noise) များကို လျှော့ချပေးသည်။')
Rigor = input.int(5, title = 'Strategy Rigor', group = gr_Basic_Settings, tooltip = 'Entry ဝင်မည့် စည်းမျဉ်းများ၏ တင်းကျပ်မှုကို သတ်မှတ်သည်။ တန်ဖိုးမြင့်ပါက Signal ပြသမှုနည်းသော်လည်း ပိုမိုတိကျနိုင်သည်။')

Select_Color_Pallet = input.string(defval = 'None', title = 'Color Theme:', options = ['None', 'Default', 'Bright', 'Lune', 'Ice'], inline = 'Color Theme', group = gr_Basic_Settings)
Trade_Bar_Color = input.bool(false, title = 'Color Candles based on Trade', group = gr_Basic_Settings)

// Global Indicator Calculations (Moved out of 'if' blocks to prevent errors)
atr14 = ta.atr(14)
avgVol = ta.sma(volume, 20)

//==============================================================================
//COLOR THEMES
//==============================================================================
Light_Pallet = array.new_color(7, na)
array.set(Light_Pallet, 0, color.new(#5BFF29, 0))
array.set(Light_Pallet, 1, color.new(#affc41, 0))
array.set(Light_Pallet, 2, color.new(#FF2937, 0))
array.set(Light_Pallet, 3, color.new(#FF8629, 0))
array.set(Light_Pallet, 4, color.new(#B029FF, 0))
array.set(Light_Pallet, 5, color.new(#FF29B8, 0))
array.set(Light_Pallet, 6, color.new(#29F8FF, 0))

Default_Pallet = array.new_color(7, na)
array.set(Default_Pallet, 0, color.new(#22c829, 0))
array.set(Default_Pallet, 1, color.new(#22c829, 0))
array.set(Default_Pallet, 2, color.new(#d31d1d, 0))
array.set(Default_Pallet, 3, color.new(#d31d1d, 0))
array.set(Default_Pallet, 4, color.new(color.purple, 0))
array.set(Default_Pallet, 5, color.new(color.maroon, 0))
array.set(Default_Pallet, 6, color.new(color.blue, 0))

Lune_Pallet = array.new_color(7, na)
array.set(Lune_Pallet, 0, color.new(color.green, 0))
array.set(Lune_Pallet, 1, color.new(color.green, 0))
array.set(Lune_Pallet, 2, color.new(color.red, 0))
array.set(Lune_Pallet, 3, color.new(color.red, 0))
array.set(Lune_Pallet, 4, color.new(#05e1b9, 0))
array.set(Lune_Pallet, 5, color.new(#14b0e7, 0))
array.set(Lune_Pallet, 6, color.new(#C4AD83, 0))

Ice_Pallet = array.new_color(7, na)
array.set(Ice_Pallet, 0, color.new(#007BBA, 0))
array.set(Ice_Pallet, 1, color.new(#0194fe, 0))
array.set(Ice_Pallet, 2, color.new(#F1F2F6, 0))
array.set(Ice_Pallet, 3, color.new(#B2B0B0, 0))
array.set(Ice_Pallet, 4, color.new(#00A9E2, 0))
array.set(Ice_Pallet, 5, color.new(#EBEBEB, 0))
array.set(Ice_Pallet, 6, color.new(#FF8600, 0))

NA_Palett = array.new_color(7, na)
array.set(NA_Palett, 0, color.new(color.green, 0))
array.set(NA_Palett, 1, color.new(color.green, 0))
array.set(NA_Palett, 2, color.new(color.red, 0))
array.set(NA_Palett, 3, color.new(color.red, 0))
array.set(NA_Palett, 4, color.new(color.purple, 0))
array.set(NA_Palett, 5, color.new(color.maroon, 0))
array.set(NA_Palett, 6, color.new(color.blue, 0))

Color_Pallet = switch Select_Color_Pallet
    'Bright' => Light_Pallet
    'Default' => Default_Pallet
    'Lune' => Lune_Pallet
    'Ice' => Ice_Pallet
    => NA_Palett // Default Fix

color_1 = array.get(Color_Pallet, 0) 
color_2 = array.get(Color_Pallet, 1) 
color_3 = array.get(Color_Pallet, 2) 
color_4 = array.get(Color_Pallet, 3) 
color_5 = array.get(Color_Pallet, 4) 
color_6 = array.get(Color_Pallet, 5) 
color_7 = array.get(Color_Pallet, 6) 

// Features
gr_Features = '📋Features📋'
Plot_SSL = false
Plot_MAs = input.bool(defval = false, title = 'Plot EK Clouds          ', group = gr_Features, inline = 'Features 3')
Plot_Revs = input.bool(defval = false, title = 'Plot Reversal Bands', group = gr_Features, inline = 'Features 3')
Plot_Vol_Pro = input.bool(false, title = 'Plot Vray                    ', group = gr_Features, inline = 'Features 1')
Plot_Engulf = input.bool(false, title = 'Plot Engulfing Signals   ', group = gr_Features, inline = 'Features 1')
plot_Lune = input.bool(defval = false, title = 'Plot Lune-Confirmation', group = gr_Features, inline = 'Features 4')
Reverse_signals = input.bool(false, title = 'Plot Reversal Signals', group = gr_Features, inline = 'Features 4')

// Advanced settings
GR_Adv_Settings = 'Advanced Settings'
Dashboard_Loc_1 = input.string('Bottom', title = 'Dashboard Location:', options = ['Bottom', 'Middle', 'Top', 'None'], group = GR_Adv_Settings, inline = 'Dashboard')
Dashboard_Loc_2 = input.string('Right', title = '-', options = ['Right', 'Center', 'Left', 'None'], group = GR_Adv_Settings, inline = 'Dashboard')
Stats_Loc_1 = input.string('Top', title = 'Statistics Location:  ', options = ['Bottom', 'Middle', 'Top', 'None'], group = GR_Adv_Settings, inline = 'Dashboard')
Stats_Loc_2 = input.string('Right', title = '-', options = ['Right', 'Center', 'Left', 'None'], group = GR_Adv_Settings, inline = 'Dashboard')

var string Dashboard_Position = na
if Dashboard_Loc_1 == 'Bottom' and Dashboard_Loc_2 == 'Right'
    Dashboard_Position := position.bottom_right
else if Dashboard_Loc_1 == 'Middle' and Dashboard_Loc_2 == 'Right'
    Dashboard_Position := position.middle_right
else if Dashboard_Loc_1 == 'Top' and Dashboard_Loc_2 == 'Right'
    Dashboard_Position := position.top_right
else if Dashboard_Loc_1 == 'Bottom' and Dashboard_Loc_2 == 'Center'
    Dashboard_Position := position.bottom_center
else if Dashboard_Loc_1 == 'Middle' and Dashboard_Loc_2 == 'Center'
    Dashboard_Position := position.middle_center
else if Dashboard_Loc_1 == 'Top' and Dashboard_Loc_2 == 'Center'
    Dashboard_Position := position.top_center
else if Dashboard_Loc_1 == 'Bottom' and Dashboard_Loc_2 == 'Left'
    Dashboard_Position := position.bottom_left
else if Dashboard_Loc_1 == 'Middle' and Dashboard_Loc_2 == 'Left'
    Dashboard_Position := position.middle_left
else if Dashboard_Loc_1 == 'Top' and Dashboard_Loc_2 == 'Left'
    Dashboard_Position := position.top_left
else
    Dashboard_Position := position.top_left

var string Stats_Position = na
if Stats_Loc_1 == 'Bottom' and Stats_Loc_2 == 'Right'
    Stats_Position := position.bottom_right
else if Stats_Loc_1 == 'Middle' and Stats_Loc_2 == 'Right'
    Stats_Position := position.middle_right
else if Stats_Loc_1 == 'Top' and Stats_Loc_2 == 'Right'
    Stats_Position := position.top_right
else if Stats_Loc_1 == 'Bottom' and Stats_Loc_2 == 'Center'
    Stats_Position := position.bottom_center
else if Stats_Loc_1 == 'Middle' and Stats_Loc_2 == 'Center'
    Stats_Position := position.middle_center
else if Stats_Loc_1 == 'Top' and Stats_Loc_2 == 'Center'
    Stats_Position := position.top_center
else if Stats_Loc_1 == 'Bottom' and Stats_Loc_2 == 'Left'
    Stats_Position := position.bottom_left
else if Stats_Loc_1 == 'Middle' and Stats_Loc_2 == 'Left'
    Stats_Position := position.middle_left
else if Stats_Loc_1 == 'Top' and Stats_Loc_2 == 'Left'
    Stats_Position := position.top_left
else
    Stats_Position := position.top_left

// Variables
var bool InLong = false
var bool InShort = false
var bool InLong_Sig = false
var bool InShort_Sig = false
var bool InTrade = false
var float Entry_level = na
var float nLoss = na
var float nProfit_1 = na
var float short_stop_level = na
var float long_stop_level = na
var float long_profit_level_1 = na
var float long_profit_level_2 = na
var float long_profit_level_3 = na
var float short_profit_level_1 = na
var float short_profit_level_2 = na
var float short_profit_level_3 = na

var Bars_In_Trade = 0
var Long_Trades = 0
var Short_Trades = 0
var SL_Exit = 0
var TP_1 = 0
var TP_2 = 0
var TP_3 = 0

//=============================================================================
// INDICATOR 1 - Entry Trigger
//=============================================================================
Timeframe = ''
Repaint = false
MA_T1 = 'Ehlers Kaufman'
MA_S1_Input = close
MA_L1 = Sensitivity
MA_T2 = 'Ehlers Kaufman'
MA_S2_Input = close
MA_L2 = Rigor
MA_S1 = request.security(syminfo.tickerid, Timeframe, MA_S1_Input[Repaint ? 0 : barstate.isrealtime ? 1 : 0])[Repaint ? 0 : barstate.isrealtime ? 0 : 1]
MA_S2 = request.security(syminfo.tickerid, Timeframe, MA_S2_Input[Repaint ? 0 : barstate.isrealtime ? 1 : 0])[Repaint ? 0 : barstate.isrealtime ? 0 : 1]

// ---- Moving Averages ----
MA_1 = switch MA_T1
    'Simple' => ta.sma(MA_S1, MA_L1)
    'Exponential' => ta.ema(MA_S1, MA_L1)
    'Double Exponential' => 2 * ta.ema(MA_S1, MA_L1) - ta.ema(ta.ema(MA_S1, MA_L1), MA_L1)
    'Triple Exponential' => 3 * (ta.ema(MA_S1, MA_L1) - ta.ema(ta.ema(MA_S1, MA_L1), MA_L1)) + ta.ema(ta.ema(ta.ema(MA_S1, MA_L1), MA_L1), MA_L1)
    'Quadruple Exponential' => 5 * ta.ema(MA_S1, MA_L1) - 10 * ta.ema(ta.ema(MA_S1, MA_L1), MA_L1) + 10 * ta.ema(ta.ema(ta.ema(MA_S1, MA_L1), MA_L1), MA_L1) - 5 * ta.ema(ta.ema(ta.ema(ta.ema(MA_S1, MA_L1), MA_L1), MA_L1), MA_L1) + ta.ema(ta.ema(ta.ema(ta.ema(ta.ema(MA_S1, MA_L1), MA_L1), MA_L1), MA_L1), MA_L1)
    'Weighted' => ta.wma(MA_S1, MA_L1)
    'Volume-weighted' => ta.vwma(MA_S1, MA_L1)
    'Hull' => ta.hma(MA_S1, MA_L1)
    'Symmetrical' => ta.swma(MA_S1)
    'Arnaud Legoux' => ta.alma(MA_S1, MA_L1, 0.85, 6)
    'Least Squares' => ta.linreg(MA_S1, MA_L1, 0)
    'Relative Strength' => ta.rma(MA_S1, MA_L1)
    'Welles Wilder' => 
        Wilder_MA1 = .0
        Wilder_MA1 := 1 / MA_L1 * MA_S1 + (1 - 1 / MA_L1) * nz(Wilder_MA1[1])
        Wilder_MA1
    'Triangular' => ta.sma(ta.sma(MA_S1, MA_L1), MA_L1)
    'Ehlers Kaufman' => 
        KA_D1 = .0
        for int i = 0 to 80 - 1 by 1
            KA_D1 := KA_D1 + math.abs(nz(MA_S1[i]) - nz(MA_S1[i + 1]))
        KA_EF1 = KA_D1 != 0 ? math.min(math.abs(MA_S1 - nz(MA_S1[80 - 1])) / KA_D1, 1) : 0
        KAMA1 = .0
        KAMA1 := math.pow(0.6667 * KA_EF1 + 0.0645, 2) * MA_S1 + (1 - math.pow(0.6667 * KA_EF1 + 0.0645, 2)) * nz(KAMA1[1], MA_S1)
        KAMA1
    => ta.sma(MA_S1, MA_L1) // Default Fix

MA_2 = switch MA_T2
    'Simple' => ta.sma(MA_S2, MA_L2)
    'Exponential' => ta.ema(MA_S2, MA_L2)
    'Double Exponential' => 2 * ta.ema(MA_S2, MA_L2) - ta.ema(ta.ema(MA_S2, MA_L2), MA_L2)
    'Triple Exponential' => 3 * (ta.ema(MA_S2, MA_L2) - ta.ema(ta.ema(MA_S2, MA_L2), MA_L2)) + ta.ema(ta.ema(ta.ema(MA_S2, MA_L2), MA_L2), MA_L2)
    'Quadruple Exponential' => 5 * ta.ema(MA_S2, MA_L2) - 10 * ta.ema(ta.ema(MA_S2, MA_L2), MA_L2) + 10 * ta.ema(ta.ema(ta.ema(MA_S2, MA_L2), MA_L2), MA_L2) - 5 * ta.ema(ta.ema(ta.ema(ta.ema(MA_S2, MA_L2), MA_L2), MA_L2), MA_L2) + ta.ema(ta.ema(ta.ema(ta.ema(ta.ema(MA_S2, MA_L2), MA_L2), MA_L2), MA_L2), MA_L2)
    'Weighted' => ta.wma(MA_S2, MA_L2)
    'Volume-weighted' => ta.vwma(MA_S2, MA_L2)
    'Hull' => ta.hma(MA_S2, MA_L2)
    'Symmetrical' => ta.swma(MA_S2)
    'Arnaud Legoux' => ta.alma(MA_S2, MA_L2, 0.85, 6)
    'Least Squares' => ta.linreg(MA_S2, MA_L2, 0)
    'Relative Strength' => ta.rma(MA_S2, MA_L2)
    'Welles Wilder' => 
        Wilder_MA2 = .0
        Wilder_MA2 := 1 / MA_L2 * MA_S2 + (1 - 1 / MA_L2) * nz(Wilder_MA2[1])
        Wilder_MA2
    'Triangular' => ta.sma(ta.sma(MA_S2, MA_L2), MA_L2)
    'Ehlers Kaufman' => 
        KA_D2 = .0
        for int i = 0 to 135 - 1 by 1
            KA_D2 := KA_D2 + math.abs(nz(MA_S2[i]) - nz(MA_S2[i + 1]))
        KA_EF2 = KA_D2 != 0 ? math.min(math.abs(MA_S2 - nz(MA_S2[135 - 1])) / KA_D2, 1) : 0
        KAMA2 = .0
        KAMA2 := math.pow(0.6667 * KA_EF2 + 0.0645, 2) * MA_S2 + (1 - math.pow(0.6667 * KA_EF2 + 0.0645, 2)) * nz(KAMA2[1], MA_S2)
        KAMA2
    => ta.sma(MA_S2, MA_L2)

MA_Color = Plot_MAs ? MA_1 > MA_2 ? color.new(color_1, 80) : color.new(color_3, 80) : na
P1 = plot(Plot_MAs ? MA_1 : na, title = 'Fast MA', color = MA_Color)
P2 = plot(Plot_MAs ? MA_2 : na, title = 'Slow MA', color = MA_Color)
fill(P1, P2, color = MA_Color)

//=============================================================================
// INDICATOR 2 - Filter
//=============================================================================
lookbackLength_1m = 500
numRows_1m = 250
lookbackLength_fallback = 200
numRows_fallback = 250
PoCThickness = 2
PoCColor = color_7
lowVolColor = color.new(color.aqua, 65)
highVolColor = color.new(color.white, 100)
gradientMult = 6

isNewPeriod = ta.change(time('D'))
var float _chartTfInMinutes = timeframe.multiplier * (timeframe.isseconds ? 1. / 60 : timeframe.isminutes ? 1. : timeframe.isdaily ? 60. * 24 : timeframe.isweekly ? 60. * 24 * 7 : timeframe.ismonthly ? 60. * 24 * 30.4375 : na)

lookbackLength = _chartTfInMinutes < 5 ? lookbackLength_1m : lookbackLength_fallback
numRows = _chartTfInMinutes < 5 ? numRows_1m : numRows_fallback

var rowsBoxes = array.new_box()
if barstate.isfirst
    for i = 0 to numRows - 1 by 1
        array.push(rowsBoxes, box.new(na, na, na, na))
var pocLine = line.new(na, na, na, na, width = 2)

highest = ta.highest(lookbackLength)
lowest = ta.lowest(lookbackLength)
priceRange = (highest - lowest) / numRows

line poc = na
box rowBox = na
levels = array.new_float(0)
sumVol = array.new_float(0)

if barstate.islast and Plot_Vol_Pro
    for i = 0 to numRows by 1
        array.push(levels, lowest + i / numRows * (highest - lowest))
    for j = 0 to numRows - 1 by 1
        sum = 0.
        for k = 0 to lookbackLength - 1 by 1
            sum := high[k] > array.get(levels, j) and low[k] < array.get(levels, j + 1) ? sum + volume[k] : sum
        array.push(sumVol, sum)
        
    maxVol = array.max(sumVol) 
    for j = 0 to numRows - 1 by 1
        mult = maxVol > 0 ? math.pow(array.get(sumVol, j) / maxVol, gradientMult) : 0 
        rowBox := array.get(rowsBoxes, j)
        volumeLevel = array.get(levels, j)

        box.set_lefttop(rowBox, bar_index - lookbackLength, volumeLevel - priceRange / 2)
        box.set_rightbottom(rowBox, bar_index, volumeLevel + priceRange / 2)
        box.set_bgcolor(rowBox, color.from_gradient(mult * 99, 0, 100, highVolColor, lowVolColor))
        box.set_border_width(rowBox, 0)

        if mult == 1
            avg = math.avg(volumeLevel, array.get(levels, j + 1))
            line.set_xy1(pocLine, bar_index - lookbackLength + 1, avg)
            line.set_xy2(pocLine, bar_index, avg)
            line.set_color(pocLine, PoCColor)
            line.set_style(pocLine, line.style_dashed)
            line.set_width(pocLine, PoCThickness)

//=============================================================================
// INDICATOR 4 - RSI
//=============================================================================
rsiLengthInput = 14
rsiSourceInput = close
up = ta.rma(math.max(ta.change(rsiSourceInput), 0), rsiLengthInput)
down = ta.rma(-math.min(ta.change(rsiSourceInput), 0), rsiLengthInput)
rsi_Ind = down == 0 ? 100 : up == 0 ? 0 : 100 - 100 / (1 + up / down)

//==============================================================================
//INDICATOR 5 - Engulfing /Bull/Bear
//==============================================================================
label_color_bullish = color_2
label_color_bearish = color_4
C_Len = 14 
C_ShadowPercent = 5.0 
C_ShadowEqualsPercent = 100.0
C_DojiBodyPercent = 5.0

C_BodyHi = math.max(close, open)
C_BodyLo = math.min(close, open)
C_Body = C_BodyHi - C_BodyLo
C_BodyAvg = ta.ema(C_Body, C_Len)
C_SmallBody = C_Body < C_BodyAvg
C_LongBody = C_Body > C_BodyAvg
C_UpShadow = high - C_BodyHi
C_DnShadow = C_BodyLo - low
C_HasUpShadow = C_UpShadow > C_ShadowPercent / 100 * C_Body
C_HasDnShadow = C_DnShadow > C_ShadowPercent / 100 * C_Body
C_WhiteBody = open < close
C_BlackBody = open > close
C_Range = high - low
C_IsInsideBar = C_BodyHi[1] > C_BodyHi and C_BodyLo[1] < C_BodyLo
C_BodyMiddle = C_Body / 2 + C_BodyLo
C_ShadowEquals = C_UpShadow == C_DnShadow or math.abs(C_UpShadow - C_DnShadow) / C_DnShadow * 100 < C_ShadowEqualsPercent and math.abs(C_DnShadow - C_UpShadow) / C_UpShadow * 100 < C_ShadowEqualsPercent
C_IsDojiBody = C_Range > 0 and C_Body <= C_Range * C_DojiBodyPercent / 100
C_Doji = C_IsDojiBody and C_ShadowEquals

C_EngulfingBullish = C_WhiteBody and C_LongBody and C_BlackBody[1] and C_SmallBody[1] and close >= open[1] and open <= close[1] and (close > open[1] or open < close[1])
C_EngulfingBearish = C_BlackBody and C_LongBody and C_WhiteBody[1] and C_SmallBody[1] and close <= open[1] and open >= close[1] and (close < open[1] or open > close[1])

plotshape(C_EngulfingBullish and Plot_Engulf ? close : na, title = 'Bullish Engulf', style = shape.triangleup, location = location.belowbar, size = size.tiny, color = label_color_bullish)
plotshape(C_EngulfingBearish and Plot_Engulf ? close : na, title = 'Bearish Engulf', style = shape.triangledown, location = location.abovebar, size = size.tiny, color = label_color_bearish)

//==============================================================================
//INDICATOR 7 - Momentum Squeeze
//==============================================================================
MOM_length = 20
MOM_mult = 2.0
MOM_lengthKC = 20
MOM_multKC = 1.5
MOM_source = close
basis = ta.sma(MOM_source, MOM_length)
dev = MOM_multKC * ta.stdev(MOM_source, MOM_length)
upperBB = basis + dev
lowerBB = basis - dev
ma = ta.sma(MOM_source, MOM_lengthKC)
range_1 = ta.tr 
rangema = ta.sma(range_1, MOM_lengthKC)
upperKC = ma + rangema * MOM_multKC
lowerKC = ma - rangema * MOM_multKC

sqzOn = lowerBB > lowerKC and upperBB < upperKC
sqzOff = lowerBB < lowerKC and upperBB > upperKC
noSqz = sqzOn == false and sqzOff == false

val = ta.linreg(MOM_source - math.avg(math.avg(ta.highest(high, MOM_lengthKC), ta.lowest(low, MOM_lengthKC)), ta.sma(close, MOM_lengthKC)), MOM_lengthKC, 0)
scolor = noSqz ? color.blue : sqzOn ? color.black : color.gray

MOM_Signal_Long = scolor == color.gray and scolor[1] != color.gray and val > 0
MOM_Signal_Short = scolor == color.gray and scolor[1] != color.gray and val < 0

//==============================================================================
//INDICATOR 8 - Market Volitility
//==============================================================================
ATR_Fast = ta.rma(ta.tr(true), 20)
ATR_Slow = ta.rma(ta.tr(true), 200)
Volitility = ATR_Fast > ATR_Slow ? 'High' : 'Low'

//=============================================================================
// INDICATOR 9 - SSL
//=============================================================================
SSL_len = 10
SSL_smaHigh = ta.sma(high, SSL_len)
SSL_smaLow = ta.sma(low, SSL_len)
Hlv = int(na)
Hlv := close > SSL_smaHigh ? 1 : close < SSL_smaLow ? -1 : Hlv[1]
sslDown = Hlv < 0 ? SSL_smaHigh : SSL_smaLow
sslUp = Hlv < 0 ? SSL_smaLow : SSL_smaHigh

SSL_Color = sslUp > sslDown ? color_5 : color_6
plot(Plot_SSL ? sslDown : na, linewidth = 2, color = SSL_Color)
plot(Plot_SSL ? sslUp : na, linewidth = 2, color = SSL_Color)

//=============================================================================
// INDICATOR 10 - TDFI
//============================================================================
trendPeriod = 20
maType = 'JMA' 
maPeriod = 8
triggerUp = 0.05 
triggerDown = -0.05 

mma = ta.ema(close, trendPeriod)
smma = ta.ema(mma, trendPeriod)
impetmma = ta.change(mma)
impetsmma = ta.change(smma)
divma = math.abs(mma - smma) / syminfo.mintick
averimpet = math.avg(impetmma, impetsmma) / (2 * syminfo.mintick)

tdfRaw = divma * math.pow(averimpet, 3)
tdfAbsRaw = math.abs(tdfRaw)

for i = 1 to 3 * trendPeriod - 1 by 1
    cand = math.abs(nz(tdfRaw[i]))
    tdfAbsRaw := cand > tdfAbsRaw ? cand : tdfAbsRaw

ratio = tdfRaw / tdfAbsRaw
smooth = float(na)

_jma(src, length) =>
    alpha = 0.45 * (length - 1) / (0.45 * (length - 1) + 2)
    out = 0.0
    e0 = 0.0
    e0 := (1 - alpha) * src + alpha * nz(e0[1])
    e1 = 0.0
    e1 := (1 - alpha) * (src - e0) + alpha * nz(e1[1])
    e2 = 0.0
    e2 := math.pow(1 - alpha, 2) * (e0 + e1 - nz(out[1])) + math.pow(alpha, 2) * nz(e2[1])
    out := e2 + nz(out[1])
    out

_jma__1 = _jma(ratio, maPeriod)
smooth := maType == 'JMA' ? _jma__1 : smooth
tdf = tdfAbsRaw > 0 ? math.max(math.min(na(smooth) ? ratio : smooth, 1), -1) : 0.0

//=============================================================================
// INDICATOR 11 - Trend Confidence & ADX
//============================================================================
TM_Long = ta.cci(close, 5) > 0
TM_Short = ta.cci(close, 5) < 0

lenadx = 14
lensig = 14
limadx = 18

ADX_up = ta.change(high)
ADX_down = -ta.change(low)
trur = ta.rma(ta.tr, lenadx)
plus = fixnan(100 * ta.rma(ADX_up > ADX_down and ADX_up > 0 ? ADX_up : 0, lenadx) / trur)
minus = fixnan(100 * ta.rma(ADX_down > ADX_up and ADX_down > 0 ? ADX_down : 0, lenadx) / trur)
sum = plus + minus
adx = 100 * ta.rma(math.abs(plus - minus) / (sum == 0 ? 1 : sum), lensig)

ADX_Long = adx > limadx and plus > minus
ADX_Short = adx > limadx and plus < minus

ACC_Dist = ta.sma(ta.accdist, 21)
ACC_Long = ta.accdist > ACC_Dist
ACC_Short = ta.accdist < ACC_Dist

MFI = ta.mfi(close, 14)
MFI_SMA = ta.sma(MFI, 9)

MFI_Long = MFI > MFI_SMA
MFI_Short = MFI < MFI_SMA

mom = ta.mom(close, 10)
lrmom = ta.linreg(mom, 14, 0)
MOML_Long = lrmom > lrmom[1]
MOML_Short = lrmom < lrmom[1]

//=============================================================================
// INDICATOR 12 - Reversal Zones
//============================================================================
bb_length = 70
bb_source = close
bb_mult = 2
bb_mult_inc = 0.75
break_mult = 5
breakhigh_source = high
breaklow_source = low

ema_1 = ta.ema(bb_source, bb_length)
sma_1 = ta.sma(bb_source, bb_length)
bb_basis = ema_1 

Rev_dev = ta.stdev(bb_source, bb_length)
bb_dev_inner = bb_mult * Rev_dev
bb_dev_mid = (bb_mult + bb_mult_inc) * Rev_dev
bb_dev_outer = (bb_mult + bb_mult_inc * 2) * Rev_dev
break_dev = break_mult * Rev_dev

inner_high = bb_basis + bb_dev_inner
mid_high = bb_basis + bb_dev_mid
outer_high = bb_basis + bb_dev_outer
inner_low = bb_basis - bb_dev_inner
mid_low = bb_basis - bb_dev_mid
outer_low = bb_basis - bb_dev_outer
break_high = bb_basis + break_dev
break_low = bb_basis - break_dev

ubi = plot(Plot_Revs ? inner_high : na, title = 'Upper Band Inner', color = color.new(color_3, 90))
ubm = plot(Plot_Revs ? mid_high : na, title = 'Upper Band Middle', color = color.new(color_3, 85))
ubo = plot(Plot_Revs ? outer_high : na, title = 'Upper Band Outer', color = color.new(color_3, 80))
fill(ubi, ubm, title = 'Upper Bands Inner Fill', color = color.new(color_3, 90))
fill(ubm, ubo, title = 'Upper Bands Outer Fill', color = color.new(color_4, 80))

lbi = plot(Plot_Revs ? inner_low : na, title = 'Lower Band Inner', color = color.new(color_1, 90))
lbm = plot(Plot_Revs ? mid_low : na, title = 'Lower Band Middle', color = color.new(color_1, 85))
lbo = plot(Plot_Revs ? outer_low : na, title = 'Lower Band Outer', color = color.new(color_1, 80))
fill(lbi, lbm, title = 'Lower Bands Inner Fill', color = color.new(color_1, 90))
fill(lbm, lbo, title = 'Lower Bands Outer Fill', color = color.new(color_2, 80))

plotshape(Reverse_signals ? breakhigh_source >= break_high : false, title = 'High Breakout', style = shape.triangledown, location = location.abovebar, size = size.tiny, color = color.new(color_3, 0))
plotshape(Reverse_signals ? breaklow_source <= break_low : false, title = 'Low Breakout', style = shape.triangleup, location = location.belowbar, size = size.tiny, color = color.new(color_1, 0))

//=============================================================================
// INDICATOR 13 - Kama/Vidya
//============================================================================
Vid_length = Rigor 
getCMO(src, length) =>
    Vid_mom = ta.change(src)
    upSum = math.sum(math.max(Vid_mom, 0), length)
    downSum = math.sum(-math.min(Vid_mom, 0), length)
    out = (upSum - downSum) / (upSum + downSum)
    out

cmo = math.abs(getCMO(close, Vid_length))
Vidya_alpha = 2 / (Vid_length + 1)
vidya = 0.0
vidya := close * Vidya_alpha * cmo + nz(vidya[1]) * (1 - Vidya_alpha * cmo)

vidyaColor = vidya > vidya[1] ? color_1 : color_3 
plot(plot_Lune ? vidya : na, title = 'Plot', linewidth = 2, color = vidyaColor)

Kama_length = Sensitivity
fastLength = 2.0
slowLength = 10.0

kama_mom = math.abs(ta.change(close, Kama_length))
volatility = math.sum(math.abs(ta.change(close)), Kama_length)
er = volatility != 0 ? kama_mom / volatility : 0
fastAlpha = 2 / (fastLength + 1)
slowAlpha = 2 / (slowLength + 1)
alpha = math.pow(er * (fastAlpha - slowAlpha) + slowAlpha, 2)

kama = 0.0
kama := alpha * close + (1 - alpha) * nz(kama[1], close)

await = barstate.isconfirmed 
maColor = kama > kama[1] and await ? color_1 : color_3 
plot(plot_Lune ? kama : na, title = 'Plot', linewidth = 2, color = maColor)

//==============================================================================
//ENTRY CONDITIONS - Logic
//==============================================================================
gr_Exit = '💵Take Profit/Stop Loss Settings💵'
Target_Exit = input.string('Percent (%)', title = 'Exit Target Method   ', options = ['Percent (%)', 'ATR', 'Chandelier', 'Off'], group = gr_Exit, tooltip = 'Take Profit နှင့် Stop Loss တွက်ချက်မည့်နည်းလမ်းကို ရွေးချယ်ပါ:\n- Percent (%): ဝင်ရောက်သည့် ဈေးနှုန်းပေါ်မူတည်၍ ပုံသေရာခိုင်နှုန်းဖြင့် တွက်သည်။\n- ATR: လက်ရှိ ဈေးကွက် အတက်အကျ (Volatility) ပေါ်မူတည်၍ အလိုအလျောက် ချိန်ညှိပေးသည်။')

TP_Level_1 = input.float(defval = 0.5, title = 'TP Level 1/2/3', step = 0.1, group = gr_Exit, inline = 'TP', tooltip = 'ပထမ Take Profit ပစ်မှတ် (ကနဦးအမြတ်ကို ရယူရန်)။') * 0.01
TP_Level_2 = input.float(defval = 1, title = '/', step = 0.1, group = gr_Exit, inline = 'TP', tooltip = 'ဒုတိယ Take Profit ပစ်မှတ်။') * 0.01
TP_Level_3 = input.float(defval = 1.5, title = '/', step = 0.1, group = gr_Exit, inline = 'TP', tooltip = 'တတိယ Take Profit ပစ်မှတ် (အမြတ်ပိုမိုရယူနိုင်ရန် ချန်ထားမည့် အနေအထား)။') * 0.01
SL_Level_1 = input.float(defval = 1.5, title = 'Stop Loss', step = 0.1, group = gr_Exit, tooltip = 'တစ်ကြိမ် အရောင်းအဝယ်အတွက် လက်ခံနိုင်သော အများဆုံး အရှုံးပမာဏ။') * 0.01

ATR_Exit = ta.atr(21)

//==============================================================================
// Strategies
//==============================================================================
gr_Trade_MGT = '💻Trade Managmeent💻'
ShortSignals = input(true, 'Include Short Trades', group = gr_Trade_MGT)
LongSignals = input(true, 'Include Long Trades', group = gr_Trade_MGT)

Strategy_2_long = InTrade == false and MA_1 > MA_2 and rsi_Ind > 50 and C_EngulfingBullish and LongSignals 
Strategy_2_short = InTrade == false and MA_1 < MA_2 and rsi_Ind < 50 and C_EngulfingBearish and ShortSignals 
Strategy_2_long_Sig = MA_1 > MA_2 and rsi_Ind > 50 and C_EngulfingBullish 
Strategy_2_short_Sig = MA_1 < MA_2 and rsi_Ind < 50 and C_EngulfingBearish 

Strategy_3_long = InTrade == false and MA_1 > MA_2 and LongSignals and MOM_Signal_Long and LongSignals 
Strategy_3_short = InTrade == false and MA_1 < MA_2 and ShortSignals and MOM_Signal_Short and ShortSignals 
Strategy_3_long_Sig = MA_1 > MA_2 and LongSignals and MOM_Signal_Long
Strategy_3_short_Sig = MA_1 < MA_2 and ShortSignals and MOM_Signal_Short

Strategy_4_long = InTrade == false and sslDown < sslUp and tdf > triggerUp and ta.crossover(rsi_Ind, 65) and LongSignals 
Strategy_4_short = InTrade == false and sslDown > sslUp and tdf < triggerDown and ta.crossunder(rsi_Ind, 35) and ShortSignals 
Strategy_4_long_Sig = sslDown < sslUp and tdf > triggerUp and ta.crossover(rsi_Ind, 65)
Strategy_4_short_Sig = sslDown > sslUp and tdf < triggerDown and ta.crossunder(rsi_Ind, 35)

Strategy_5_long = InTrade == false and ta.crossover(kama, vidya) and LongSignals 
Strategy_5_short = InTrade == false and ta.crossunder(kama, vidya) and ShortSignals 
Strategy_5_long_Sig = ta.crossover(kama, vidya)
Strategy_5_short_Sig = ta.crossunder(kama, vidya)

Long_Signal = switch Strategy_Select
    'RE Beta' => Strategy_2_long
    'MS Beta' => Strategy_3_long
    'SRT Beta' => Strategy_4_long
    'Confirmation' => Strategy_5_long
    => false

Short_Signal = switch Strategy_Select
    'RE Beta' => Strategy_2_short
    'MS Beta' => Strategy_3_short
    'SRT Beta' => Strategy_4_short
    'Confirmation' => Strategy_5_short
    => false

Long_Signal_Entry = switch Strategy_Select
    'RE Beta' => Strategy_2_long_Sig
    'MS Beta' => Strategy_3_long_Sig
    'SRT Beta' => Strategy_4_long_Sig
    'Confirmation' => Strategy_5_long_Sig
    => false

Short_Signal_Entry = switch Strategy_Select
    'RE Beta' => Strategy_2_short_Sig
    'MS Beta' => Strategy_3_short_Sig
    'SRT Beta' => Strategy_4_short_Sig
    'Confirmation' => Strategy_5_short_Sig 
    => false
    
entry_long = Target_Exit != 'Off' ? Long_Signal : Long_Signal_Entry
entry_short = Target_Exit != 'Off' ? Short_Signal : Short_Signal_Entry

// Correct Array Initialization (Memory Optimize)
var TP_Levels = array.new_float()
var SL_Levels = array.new_float()
var Entry_Levels = array.new_float()

var Array_Lines = array.new_line()
var Array_Lables = array.new_label()
var Array_Lines_Old = array.new_line()
var Array_Labels_Old = array.new_label()

var float Entry_ATR = na

if InTrade
    Bars_In_Trade := Bars_In_Trade + 1
else if entry_long or entry_short
    Bars_In_Trade := 0

Long_Signal_Strength = 0
Short_Signal_Strength = 0

if entry_long
    if TM_Long
        Long_Signal_Strength := Long_Signal_Strength + 1
    if ADX_Long
        Long_Signal_Strength := Long_Signal_Strength + 1
    if ACC_Long
        Long_Signal_Strength := Long_Signal_Strength + 1
    if MFI_Long
        Long_Signal_Strength := Long_Signal_Strength + 1
    if MOML_Long
        Long_Signal_Strength := Long_Signal_Strength + 1

if entry_short
    if TM_Short
        Short_Signal_Strength := Short_Signal_Strength + 1
    if ADX_Short
        Short_Signal_Strength := Short_Signal_Strength + 1
    if ACC_Short
        Short_Signal_Strength := Short_Signal_Strength + 1
    if MFI_Short
        Short_Signal_Strength := Short_Signal_Strength + 1
    if MOML_Short
        Short_Signal_Strength := Short_Signal_Strength + 1

enter_Long_Text = '📈 Buy ' + str.tostring(Long_Signal_Strength)
enter_Short_Text = '📉 Sell ' + str.tostring(Short_Signal_Strength)

if entry_long
    label.new(bar_index, low - atr14 * 0.5, yloc = yloc.belowbar, text = enter_Long_Text, color = color.new(#00E676, 20), textcolor = color.white, style = label.style_label_up, size = size.small)
    InLong_Sig := true
    InShort_Sig := false

if entry_short
    label.new(bar_index, high + atr14 * 0.5, yloc = yloc.abovebar, text = enter_Short_Text, color = color.new(#FF1744, 20), textcolor = color.white, style = label.style_label_down, size = size.small)
    InLong_Sig := false
    InShort_Sig := true

if entry_long and InTrade == false
    Long_Trades := Long_Trades + 1
    Entry_level := close
    Entry_ATR := ATR_Exit
    InTrade := true
    nLoss := Entry_level * SL_Level_1
    nProfit_1 := Entry_level * TP_Level_1
    InLong := true

    if Target_Exit == 'Percent (%)'
        long_stop_level := math.min(Entry_level - nLoss, low - syminfo.mintick)
        long_profit_level_1 := math.max(Entry_level + Entry_level * TP_Level_1, high + syminfo.mintick)
        long_profit_level_2 := Entry_level + Entry_level * TP_Level_2
        long_profit_level_3 := Entry_level + Entry_level * TP_Level_3
    else
        long_stop_level := math.min(Entry_level - ATR_Exit * SL_Level_1 * 100, low - syminfo.mintick)
        long_profit_level_1 := math.max(Entry_level + Entry_ATR * TP_Level_1 * 100, high + syminfo.mintick)
        long_profit_level_2 := Entry_level + Entry_ATR * TP_Level_2 * 100
        long_profit_level_3 := Entry_level + Entry_ATR * TP_Level_3 * 100

    array.push(TP_Levels, long_profit_level_1)
    array.push(TP_Levels, long_profit_level_2)
    array.push(TP_Levels, long_profit_level_3)
    array.push(SL_Levels, long_stop_level)
    array.push(Entry_Levels, Entry_level)

else if entry_short and InTrade == false
    Short_Trades := Short_Trades + 1
    Entry_level := close
    Entry_ATR := ATR_Exit
    InTrade := true
    nLoss := Entry_level * SL_Level_1
    nProfit_1 := Entry_level * TP_Level_1
    InShort := true

    if Target_Exit == 'Percent (%)'
        short_stop_level := math.max(Entry_level + nLoss, high + syminfo.mintick)
        short_profit_level_1 := math.min(Entry_level - Entry_level * TP_Level_1, low - syminfo.mintick)
        short_profit_level_2 := Entry_level - Entry_level * TP_Level_2
        short_profit_level_3 := Entry_level - Entry_level * TP_Level_3
    else
        short_stop_level := math.max(Entry_level + ATR_Exit * SL_Level_1 * 100, high + syminfo.mintick)
        short_profit_level_1 := math.min(Entry_level - Entry_ATR * TP_Level_1 * 100, low - syminfo.mintick)
        short_profit_level_2 := Entry_level - Entry_ATR * TP_Level_2 * 100
        short_profit_level_3 := Entry_level - Entry_ATR * TP_Level_3 * 100

    array.push(TP_Levels, short_profit_level_1)
    array.push(TP_Levels, short_profit_level_2)
    array.push(TP_Levels, short_profit_level_3)
    array.push(SL_Levels, short_stop_level)
    array.push(Entry_Levels, Entry_level)

if (barstate.isrealtime or barstate.islast) and InTrade == true
    if InLong == true and Target_Exit != 'Off'
        for value in TP_Levels
            array.push(Array_Lines, line.new(bar_index[Bars_In_Trade], value, bar_index + 14, value, color = #00def6, width = 2))
            array.push(Array_Lables, label.new(bar_index + 14, value, '🎯 TP: ' + str.tostring(math.round_to_mintick(value)), color = #00def6, textcolor = color.white, style = label.style_label_left, size = size.small))

        for value in SL_Levels
            array.push(Array_Lines, line.new(bar_index[Bars_In_Trade], value, bar_index + 14, value, color = #ff0015, width = 2))
            array.push(Array_Lables, label.new(bar_index + 14, value, '⛔ SL: ' + str.tostring(math.round_to_mintick(value)), color = #ff0015, textcolor = color.white, style = label.style_label_left, size = size.small))

        for value in Entry_Levels
            array.push(Array_Lines, line.new(bar_index[Bars_In_Trade], value, bar_index + 14, value, color = #ffffff, width = 2))
            array.push(Array_Lables, label.new(bar_index + 14, value, '📍 Entry: ' + str.tostring(math.round_to_mintick(value)), color = color.new(#363a45, 0), textcolor = color.white, style = label.style_label_left, size = size.small))

    else if InShort == true and Target_Exit != 'Off'
        for value in TP_Levels
            array.push(Array_Lines, line.new(bar_index[Bars_In_Trade], value, bar_index + 14, value, color = #00def6, width = 2))
            array.push(Array_Lables, label.new(bar_index + 14, value, '🎯 TP: ' + str.tostring(math.round_to_mintick(value)), color = #00def6, textcolor = color.white, style = label.style_label_left, size = size.small))

        for value in SL_Levels
            array.push(Array_Lines, line.new(bar_index[Bars_In_Trade], value, bar_index + 14, value, color = #ff0015, width = 2))
            array.push(Array_Lables, label.new(bar_index + 14, value, '⛔ SL: ' + str.tostring(math.round_to_mintick(value)), color = #ff0015, textcolor = color.white, style = label.style_label_left, size = size.small))

        for value in Entry_Levels
            array.push(Array_Lines, line.new(bar_index[Bars_In_Trade], value, bar_index + 14, value, color = #ffffff, width = 2))
            array.push(Array_Lables, label.new(bar_index + 14, value, '📍 Entry: ' + str.tostring(math.round_to_mintick(value)), color = color.new(#363a45, 0), textcolor = color.white, style = label.style_label_left, size = size.small))

while array.size(Array_Lines) > 5
    Line = array.shift(Array_Lines)
    line.delete(Line)
while array.size(Array_Lables) > 5
    Label = array.shift(Array_Lables)
    label.delete(Label)

while array.size(TP_Levels) > 3
    array.remove(TP_Levels, 0)
while array.size(SL_Levels) > 1
    array.remove(SL_Levels, 0)
while array.size(Entry_Levels) > 1
    array.remove(Entry_Levels, 0)

//==============================================================================
//Exit Conditions
//==============================================================================
Exit_Long = InLong and low <= long_stop_level 
Exit_Short = InShort and high >= short_stop_level 

TP_1_Long = InLong and high >= long_profit_level_1
TP_2_Long = InLong and high >= long_profit_level_2
TP_3_Long = InLong and high >= long_profit_level_3

var int TP_Long_Condition = 0
var int TP_Short_Condition = 0

TP_1_Short = InShort and low <= short_profit_level_1
TP_2_Short = InShort and low <= short_profit_level_2
TP_3_Short = InShort and low <= short_profit_level_3

// ---- Pro UI Label Styles ----
color tpBg = color.new(#131722, 15)
color tpText = color.new(#00E676, 0)
color slBg = color.new(#131722, 15)
color slText = color.new(#FF1744, 0)
color lineTP = color.new(#00E676, 50)
color lineSL = color.new(#FF1744, 50)
color lineEntry = color.new(color.white, 50)

if TP_1_Long and TP_Long_Condition == 0 and not entry_long
    if Target_Exit != 'Off'
        array.push(Array_Labels_Old, label.new(bar_index, high, text = '✅ TP 1', color = tpBg, textcolor = tpText, style = label.style_label_down, size = size.small))
    TP_Long_Condition := 1
    TP_1 := TP_1 + 1

if TP_2_Long and TP_Long_Condition == 1
    if Target_Exit != 'Off'
        if array.size(Array_Labels_Old) > 0
            lb = array.pop(Array_Labels_Old)
            label.delete(lb)
        array.push(Array_Labels_Old, label.new(bar_index, high, text = '✅ TP 2', color = tpBg, textcolor = tpText, style = label.style_label_down, size = size.small))
    TP_Long_Condition := 2
    TP_2 := TP_2 + 1

if TP_3_Long and TP_Long_Condition == 2
    if Target_Exit != 'Off'
        if array.size(Array_Labels_Old) > 0
            lb = array.pop(Array_Labels_Old)
            label.delete(lb)
        array.push(Array_Labels_Old, label.new(bar_index, high, text = '🏆 TP 3', color = tpBg, textcolor = tpText, style = label.style_label_down, size = size.small))

        for value in TP_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineTP, width = 1, style = line.style_dotted))
        for value in SL_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineSL, width = 1, style = line.style_dotted))
        for value in Entry_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineEntry, width = 1, style = line.style_dotted))

    TP_Long_Condition := 3
    TP_3 := TP_3 + 1
    InLong := false
    InTrade := false
    Entry_level := na
    TP_Long_Condition := 0

if TP_1_Short and TP_Short_Condition == 0
    if Target_Exit != 'Off'
        array.push(Array_Labels_Old, label.new(bar_index, low, text = '✅ TP 1', color = tpBg, textcolor = tpText, style = label.style_label_up, size = size.small))
    TP_Short_Condition := 1
    TP_1 := TP_1 + 1

if TP_2_Short and TP_Short_Condition == 1
    if Target_Exit != 'Off'
        if array.size(Array_Labels_Old) > 0
            lb = array.pop(Array_Labels_Old)
            label.delete(lb)
        array.push(Array_Labels_Old, label.new(bar_index, low, text = '✅ TP 2', color = tpBg, textcolor = tpText, style = label.style_label_up, size = size.small))
    TP_Short_Condition := 2
    TP_2 := TP_2 + 1

if TP_3_Short and TP_Short_Condition == 2
    if Target_Exit != 'Off'
        if array.size(Array_Labels_Old) > 0
            lb = array.pop(Array_Labels_Old)
            label.delete(lb)
        array.push(Array_Labels_Old, label.new(bar_index, low, text = '🏆 TP 3', color = tpBg, textcolor = tpText, style = label.style_label_up, size = size.small))

        for value in TP_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineTP, width = 1, style = line.style_dotted))
        for value in SL_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineSL, width = 1, style = line.style_dotted))
        for value in Entry_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineEntry, width = 1, style = line.style_dotted))

    TP_Short_Condition := 3
    TP_3 := TP_3 + 1
    InShort := false
    InTrade := false
    Entry_level := na
    TP_Short_Condition := 0

if InTrade == true and Exit_Long
    if Target_Exit != 'Off'
        if TP_Long_Condition == 0
            array.push(Array_Labels_Old, label.new(bar_index, low, text = '❌ SL', color = slBg, textcolor = slText, style = label.style_label_up, size = size.small))

        for value in TP_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineTP, width = 1, style = line.style_dotted))
        for value in SL_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineSL, width = 1, style = line.style_dotted))
        for value in Entry_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineEntry, width = 1, style = line.style_dotted))

    InLong := false
    InTrade := false
    Entry_level := na
    TP_Long_Condition := 0
    SL_Exit := SL_Exit + 1

else if InTrade == true and Exit_Short
    if Target_Exit != 'Off'
        if TP_Short_Condition == 0
            array.push(Array_Labels_Old, label.new(bar_index, high, text = '❌ SL', color = slBg, textcolor = slText, style = label.style_label_down, size = size.small))

        for value in TP_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineTP, width = 1, style = line.style_dotted))
        for value in SL_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineSL, width = 1, style = line.style_dotted))
        for value in Entry_Levels
            array.push(Array_Lines_Old, line.new(bar_index[Bars_In_Trade], value, bar_index, value, color = lineEntry, width = 1, style = line.style_dotted))

    InShort := false
    InTrade := false
    Entry_level := na
    TP_Short_Condition := 0
    SL_Exit := SL_Exit + 1

// Memory Cleanup Optimization
while array.size(Array_Lines_Old) > 100
    LineOld = array.shift(Array_Lines_Old)
    line.delete(LineOld)

while array.size(Array_Labels_Old) > 100
    LabelOld = array.shift(Array_Labels_Old)
    label.delete(LabelOld)

//=============================================================================
// BAR COLOR
//=============================================================================
Green = close > open
Candle_Colors = Select_Color_Pallet == 'None' ? na : InLong_Sig and Target_Exit == 'Off' and Trade_Bar_Color ? color_2 : InShort_Sig and Target_Exit == 'Off' and Trade_Bar_Color ? color_4 : Trade_Bar_Color and InLong ? color_2 : Trade_Bar_Color and InShort ? color_4 : not InLong and not InShort or not Trade_Bar_Color ? Green ? color_1 : color_3 : na
barcolor(Candle_Colors)

// ==========================================================================================
// 🖥️ PRO-QUANT DASHBOARD & HUD 
// ==========================================================================================

Current_Strategy = Strategy_Select

// Active Trade Tracker
string activeTradeStr = not InTrade ? "STANDBY" : InLong ? "LONG ACTIVE" : "SHORT ACTIVE"
color activeTradeCol = not InTrade ? na : InLong ? color.new(#00E676, 20) : color.new(#FF5252, 20)

Market_Trend = MA_1 > MA_2 ? '▲ BULLISH' : '▼ BEARISH'
color trendCol = MA_1 > MA_2 ? color.new(#00E676, 0) : color.new(#FF5252, 0)

Market_Volitility = Volitility == 'High' ? 'EXPANDING' : 'SQUEEZE (LOW)'
color volCol = Volitility == 'High' ? color.new(#2962FF, 0) : color.new(#787b86, 0)

// Math & Performance Metrics
Total_Trades = Long_Trades + Short_Trades
Loss_perc = Total_Trades > 0 ? math.round((Total_Trades - TP_1) / Total_Trades * 100, 2) : 0
Win_Rate = Total_Trades > 0 ? 100 - Loss_perc : 0 

TP1_Perc = Total_Trades > 0 ? math.round(TP_1 / Total_Trades * 100, 2) : 0
TP2_Perc = Total_Trades > 0 ? math.round(TP_2 / Total_Trades * 100, 2) : 0
TP3_Perc = Total_Trades > 0 ? math.round(TP_3 / Total_Trades * 100, 2) : 0

BE_Rate = math.round(SL_Level_1 / (TP_Level_1 + SL_Level_1) * 100, 2)
RR_Ratio = math.round(TP_Level_1 / SL_Level_1, 2) // Risk to Reward Ratio
Profitability = TP1_Perc > BE_Rate

// Professional Color Palette
color bgPro = color.new(#080a0f, 10) // Ultra Dark Background
color borderPro = color.new(#1e222d, 0)
color headerPro = color.new(#131722, 0)
color textMuted = color.new(#8a91a4, 0)
color textWhite = color.new(#d1d4dc, 0)
color blueHighlight = color.new(#2962ff, 0)

// --- 1. SYSTEM STATUS HUD ---
var table Dashboard = table.new(position = Dashboard_Position, columns = 2, rows = 8, bgcolor = bgPro, border_width = 1, border_color = borderPro, frame_width = 1, frame_color = borderPro)

if barstate.islast and (Dashboard_Loc_1 != 'None' or Dashboard_Loc_2 != 'None')
    table.merge_cells(Dashboard, 0, 0, 1, 0)
    table.cell(Dashboard, 0, 0, 'S Y S T E M   H U D', text_color = textWhite, bgcolor = headerPro, text_size = size.small, text_halign = text.align_center)
    
    table.cell(Dashboard, 0, 1, '🤖 Algorithm', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'သင်၏ Entry များကို ဆုံးဖြတ်ပေးနေသည့် လက်ရှိ အသုံးပြုနေသော Trading Strategy ဖြစ်သည်။')
    table.cell(Dashboard, 1, 1, str.tostring(Current_Strategy), text_color = blueHighlight, text_size = size.small, text_halign = text.align_right)
    
    table.cell(Dashboard, 0, 2, '🚦 Trade Status', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'လက်ရှိတွင် LONG (အဝယ်) သို့မဟုတ် SHORT (အရောင်း) Signal ဝင်နေ/မဝင်နေ ပြသသည်။')
    table.cell(Dashboard, 1, 2, activeTradeStr, text_color = textWhite, text_size = size.small, text_halign = text.align_right, bgcolor = activeTradeCol)
    
    table.cell(Dashboard, 0, 3, '🧭 Market Bias', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'Moving Average (Fast နှင့် Slow) ဖြတ်သန်းမှုအပေါ် မူတည်ပြီး ဈေးကွက်၏ ယေဘုယျ ဦးတည်ရာကို ပြသသည်။')
    table.cell(Dashboard, 1, 3, Market_Trend, text_color = trendCol, text_size = size.small, text_halign = text.align_right)
    
    table.cell(Dashboard, 0, 4, '⚡ Volatility (ATR)', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'ATR (Average True Range) သည် ဈေးကွက်အတက်အကျကြမ်းမှုကို တိုင်းတာသည်။\n- EXPANDING: အတက်အကျကြမ်းသည် (Breakout များအတွက် ကောင်းသည်)။\n- SQUEEZE: အတက်အကျနည်းသည် (ဈေးကွက်ငြိမ်နေသည်)။')
    table.cell(Dashboard, 1, 4, str.tostring(math.round(ATR_Exit, 2)) + ' | ' + Market_Volitility, text_color = textWhite, text_size = size.small, text_halign = text.align_right)

    color adxColor = adx > 25 ? color.new(#00E676, 0) : textMuted
    string adxText = adx > 25 ? 'STRONG (' + str.tostring(math.round(adx, 1)) + ')' : 'WEAK (' + str.tostring(math.round(adx, 1)) + ')'
    table.cell(Dashboard, 0, 5, '🚀 ADX Force', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'ADX (Average Directional Index) သည် Trend ၏ အားကို တိုင်းတာသည်။\n- > 25: Trend အားကောင်းသည်။\n- < 25: Trend အားနည်းသည်/ဈေးကွက် ဘေးတိုက်သွားနေသည်။')
    table.cell(Dashboard, 1, 5, adxText, text_color = adxColor, text_size = size.small, text_halign = text.align_right)

    color rsiColor = rsi_Ind >= 70 ? color.new(#FF5252, 0) : rsi_Ind <= 30 ? color.new(#00E676, 0) : textWhite
    string rsiStr = rsi_Ind >= 70 ? 'OVERBOUGHT (' + str.tostring(math.round(rsi_Ind, 1)) + ')' : rsi_Ind <= 30 ? 'OVERSOLD (' + str.tostring(math.round(rsi_Ind, 1)) + ')' : 'NEUTRAL (' + str.tostring(math.round(rsi_Ind, 1)) + ')'
    table.cell(Dashboard, 0, 6, '⚖️ RSI Momentum', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'RSI (Relative Strength Index) ဖြစ်သည်။\n- > 70: ဝယ်အားလွန်နေသည် (ဈေးပြန်ကျနိုင်ချေရှိသည်)။\n- < 30: ရောင်းအားလွန်နေသည် (ဈေးပြန်တက်နိုင်ချေရှိသည်)။')
    table.cell(Dashboard, 1, 6, rsiStr, text_color = rsiColor, text_size = size.small, text_halign = text.align_right)

    bool highVol = volume > avgVol
    color volColor = highVol ? blueHighlight : textMuted
    string volText = highVol ? 'ABOVE AVERAGE' : 'BELOW AVERAGE'
    table.cell(Dashboard, 0, 7, '🔋 Volume Flow', text_color = textMuted, text_size = size.small, text_halign = text.align_left, tooltip = 'လက်ရှိ Volume နှင့် ပြီးခဲ့သော အကြိမ် (၂၀) ၏ ပျမ်းမျှ Volume ကို နှိုင်းယှဉ်ပြသည်။\n- ABOVE AVERAGE: အရောင်းအဝယ်လုပ်သူများပြီး အဟုန် (Momentum) ကောင်းသည်။')
    table.cell(Dashboard, 1, 7, volText, text_color = volColor, text_size = size.small, text_halign = text.align_right)

// --- 2. PERFORMANCE METRICS ---
var table Stats = table.new(position = Stats_Position, columns = 3, rows = 7, bgcolor = bgPro, border_width = 1, border_color = borderPro, frame_width = 1, frame_color = borderPro)

if Strategy_Select != 'None' and Target_Exit != 'Off' and barstate.islast and (Stats_Loc_1 != 'None' or Stats_Loc_1 != 'None')
    table.merge_cells(Stats, 0, 0, 2, 0)
    table.cell(Stats, 0, 0, '🏆 P E R F O R M A N C E 🏆', text_color = textWhite, bgcolor = headerPro, text_size = size.small, text_halign = text.align_center)
    
    table.merge_cells(Stats, 0, 1, 2, 1)
    color edgeCol = Profitability ? color.new(#00E676, 0) : color.new(#FF5252, 0)
    table.cell(Stats, 0, 1, '🎯 WIN RATE: ' + str.tostring(Win_Rate) + '%', text_color = edgeCol, text_size = size.normal, text_halign = text.align_center, bgcolor = color.new(#1e222d, 50), tooltip = 'စုစုပေါင်း အရောင်းအဝယ်များထဲမှ အနည်းဆုံး Take Profit 1 သို့ အောင်မြင်စွာ ရောက်ရှိခဲ့သော ရာခိုင်နှုန်း။')
    
    table.cell(Stats, 0, 2, '📋 TOTAL\n' + str.tostring(Total_Trades), text_color = textWhite, text_size = size.small, text_halign = text.align_center)
    table.cell(Stats, 1, 2, '🐂 LONGS\n' + str.tostring(Long_Trades), text_color = color.new(#00E676, 0), text_size = size.small, text_halign = text.align_center)
    table.cell(Stats, 2, 2, '🐻 SHORTS\n' + str.tostring(Short_Trades), text_color = color.new(#FF5252, 0), text_size = size.small, text_halign = text.align_center)
    
    table.cell(Stats, 0, 3, '✅ TP 1 HITS', text_color = textMuted, text_size = size.small, text_halign = text.align_left)
    table.merge_cells(Stats, 1, 3, 2, 3)
    table.cell(Stats, 1, 3, str.tostring(TP1_Perc) + '%', text_color = blueHighlight, text_size = size.small, text_halign = text.align_right)
    
    table.cell(Stats, 0, 4, '✅ TP 2 HITS', text_color = textMuted, text_size = size.small, text_halign = text.align_left)
    table.merge_cells(Stats, 1, 4, 2, 4)
    table.cell(Stats, 1, 4, str.tostring(TP2_Perc) + '%', text_color = blueHighlight, text_size = size.small, text_halign = text.align_right)
    
    table.cell(Stats, 0, 5, '✅ TP 3 HITS', text_color = textMuted, text_size = size.small, text_halign = text.align_left)
    table.merge_cells(Stats, 1, 5, 2, 5)
    table.cell(Stats, 1, 5, str.tostring(TP3_Perc) + '%', text_color = blueHighlight, text_size = size.small, text_halign = text.align_right)
    
    table.merge_cells(Stats, 0, 6, 2, 6)
    table.cell(Stats, 0, 6, '⚖️ Risk:Reward (1:' + str.tostring(RR_Ratio) + ')  |  Req. B.E: ' + str.tostring(BE_Rate) + '%', text_color = textMuted, text_size = size.small, bgcolor = headerPro, tooltip = "အရှုံးအမြတ်မရှိ (Break-Even) ဖြစ်ရန် လိုအပ်သော အောင်မြင်မှု ရာခိုင်နှုန်း (ယင်းထက်ကျော်ပါက အမြတ်ရမည်)။")

// Alert Conditions
alertcondition(Exit_Long, 'Long Exit', 'Long Exit Alert')
alertcondition(Exit_Short, 'Short Exit', 'Short Exit Alert')

All_Long_TP_Cond = TP_1_Long and TP_Long_Condition == 0 or TP_2_Long and TP_Long_Condition == 1 or TP_3_Long and TP_Long_Condition == 2 
All_Short_TP_Cond = TP_1_Short and TP_Short_Condition == 0 or TP_2_Short and TP_Short_Condition == 1 or TP_3_Short and TP_Short_Condition == 2 

alertcondition(All_Short_TP_Cond, 'ALL TP Short', ' Short Take Profit Alert')
alertcondition(All_Long_TP_Cond, 'ALL TP Long', ' Long Take Profit Alert')
// ==========================================================================================
// ==========================================================================================

// === Dashboard with Telegram Link ===
var table myTable = table.new(position.top_center, 1, 1, border_width=1, frame_color=color.black, bgcolor=color.white)

// Add Telegram Message to Dashboard
table.cell(myTable, 0, 0, "Join Telegram @mrexpert_ai", bgcolor=color.blue, text_color=color.white, text_size=size.normal)