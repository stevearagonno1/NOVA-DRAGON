# تسليم — 2026-09-14 (اقرأ هذا أولاً)

## أين نحن (أرقام محسوبة، لا رواية)
- المستودع صار يحوي كل الذاكرة: `research/nova_v8_out/` (874 ملف نتائج) + `data/` + `library/indicators/` (129 ملف مؤشرات) + `bot_versions/` (92 نسخة قديمة) + `hypotheses/` (ملف الفرضيات + النخبة + لوحة الخط).
- التنظيم بأمر واحد قابل لإعادة التشغيل: `python3 tools/organize_incoming.py --apply` (332 منقول، 191 نسخة حرفية مشطوبة، تحقق = 0 مفقودات). الفهرس الكامل: `INDEX.md`.
- المحرك: `nova_v8/adaptive_trend.py` — فُتح مفتاحا V3A/V3B المعطوبان (D-0012)، وcanary PASS (|Δ| = 2.8e-14).
- التدقيق (`docs/ARCHIVE-VERIFIED-2026-09-14.md`): donchian أربع فصول = **-29,022.61$**؛ adaptive = **-3,404.24$**؛ «+5,104.18$» لثلاث عملات صحيح لكنه **-4,071.67$** لستّهما ⇒ 9,176$ من انتقاء العملة.
- بوابة القبول (عامل ربح ≥1.3، ≥100 صفقة، زمن لم يُستخدم في الضبط): **لا شيء عبر**. الأقرب: XLM · donchian · سوق صاعد (عامل ربح 1.07).
- `adaptive_trend` مُوقّف عن الترشيح (D-0015): أفضل تركيبة أعطت **-495.35$** في الزمن النظيف 2021-09→2023-08.

## المهمة القادمة بالترتيب
1. **سجل الورشة**: حوّل `hypotheses/فرضيات_الباحث_النهائي.txt` إلى `docs/HYPOTHESES.md` — سطر لكل فرضية: رمزها، حالتها، أفضل رقم، الزمن المستعمل، وسبب القتل. لا حكم بدون سطر.
2. **محرك الواجهة السريع** (`docs/designs/تصميم_محرك_الواجهة.txt`) + شرط القبول الإلزامي: تطابق تام مع المسار البطيء على 20 تركيبة. بدونه لا يمكن اختبار 212 فرضية × 20 تعديل.
3. **لوحة درجات الدوران بين أزمنة السوق**: كم مرة أصاب قرار التبديل، كم تأخر، وكم كلّف. هذا هو «يتداول في كل الأحوال» بأرقام.

## قواعد لا تُكسر
- `nova_v8/**` مجمّد حتى يقول «أصلح» + قرار برقم D.
- كل رقم يُحسب من ملفات الصفقات؛ لا أرقام من الذاكرة. وكل فحص = أمر واحد قابل للتكرار.
- لا مفاتيح في المستودع أبداً. **مطلوب منه**: تغيير مفتاح Gemini ومفتاح بينانس (كانا نصاً في ~30 ملفاً قديماً على الجوال).
- 42 ملفاً مغلقاً (zip/pdf/docx) معزولة في `~/NOVA_bin_pending` — تُفحص قبل أي رفع.
- الحسابات الطويلة توضع في حارة مستقلة (`docs/lanes/`)، لا في محادثة التنظيم.
