# تدقيق 2026‑09‑13 — ما وجده العقل حين أعاد الاشتقاق بدل القراءة

الأدوات: `tools/audit_lane.py` (19/36 فحصاً نظيفاً، فشلان حرجان) · `tools/canary.py` (PASS) · قراءة `crypto_archive/BTCUSDT_1m.parquet` بـ pyarrow · فحص مصدر المحرك.
كل رقم أدناه قابل لإعادة الإنتاج بأمر واحد مذكور؛ لا شيء منقول من النصوص.

## 1) خلل هوية ملف البيانات (الأهم)

| | الموثق في `PROJECT_LOG.txt` | الموجود في المستودع فعلاً |
|---|---|---|
| صفوف `BTCUSDT_1m.parquet` | 1,578,240 | **2,629,240** |
| المدى | 2023‑09 → 2026‑08 (36 شهراً) | **2021‑09‑01 → 2026‑08‑31 (60 شهراً)** |
| دقائق مفقودة | صفر | صفر في التدريب والاختبار؛ **200 دقيقة في 2021‑09→2023‑08** (فجوتان) |
| صيغة `open_time` | فهرس تاريخي (ناتج `rebuild_archive.py`) | **عمود `int64` بالملي ثانية** (كل القيم < 1e13) |

التفسير: 2,629,240 − 1,578,240 = 1,051,000 = 730 يوماً بالضبط ⇒ الملف المرفوع هو نسخة 5 سنوات **تحتوي** شريحة الـ36 شهراً التي حسبت عليها الجولة. المحرك يقتطع بالتاريخ، لذا النتيجة أُعيدت حرفياً (الكاناري: 68 صفقة، |Δnet|=2.8e‑14). **لا تحذير أمان — لكن التوثيق كان يصف ملفاً غير المرفوع.**

**الفرصة:** يوجد داخل المستودع مسبقاً **24 شهراً مقفلة قبل التدريب (2021‑09→2023‑08)** ⇒ نافذة تحقق ثالثة حقيقية بلا تنزيل ولا حوسبة إضافية. هذا يغيّر خطة L0003 كلها (كانت ستبدأ بتنزيل بيانات).

## 2) حجم البحث مبالَغ فيه منهجياً

`audit_lane.py sweep_results/adaptive_trend`:
- A9: **469 نتيجة net مميزة من 1920 تركيبة (24%)**.
- A8: `NOVA_AT_V3B` خامل في **960/960** زوجاً (تبديله لا يغيّر شيئاً)، `V3C` خامل في 480/960، `V3A` فعّال دائماً.
- donchian: `DON_MACRO` و`DON_CHOP` خاملان في **40/40** زوجاً ⇒ 80 تركيبة = 20 نتيجة.

الأثر: إعادة جولة كاملة بنفس التغطية تحتاج **960 تركيبة بدل 1920** (~1.75h بدل ~3.5h)، وأي ادعاء «جرّبنا 1920 تركيبة» يجب أن يُقال «1920 تركيبة → 469 سلوكاً مميزاً».

## 3) فخّ BOM (درس منهجي، كلّف استنتاجاً مقلوباً)

المحرك يكتب `encoding="utf-8-sig"`؛ قارئ لا يسقط الـBOM يرى العمود الأول `\ufeffNOVA_AT_TRAIL` فيختفي محور كامل من الفحص.
ظهر لي فعلاً في أول نسخة من الأداة: «384 تركيبة فريدة من 1920» (أي 1920/5) و«16 من 80» — وكلاهما زائف. الصحيح 1920 و80. **القاعدة: D‑0011.**

## 4) عبارة «مطابقة بايت‑ببايت» غير صحيحة (وحلت محلها عبارة رقمية)

من 476 تركيبة محفوظة: 446 مطابقة نصياً، 30 تختلف في آخر خانة (28 في `win_pct`، 4 في `net`)، أقصى انحراف `|Δnet|=2.842e‑14`.
السبب المرجَّح: إعادة تحرير القيم عبر `pd.read_csv → to_csv` وتغيّر ترتيب الجمع/الصياغة بين بيئتين. **الأثر على الاستنتاج: صفر** — لكن الادعاء كان أقوى من الدليل. **القاعدة: D‑0006.**

## 5) نتيجة donchian غير قابلة لإعادة الاشتقاق من الكود المرفوع (عُزلت)

- ملفاتها تُعوِّم `NOVA_DON_MACRO/NOVA_DON_CHOP`، و**الصفر مراجع** لهما في `nova_v8/` (`grep -rn "DON_MACRO\|DON_CHOP" nova_v8/ → 0`).
- شبكة donchian في المحرك الآن = `ENTRY1(5) × EXIT1(4) × FILTER(2) = 40` لا 80.
⇒ نتائج donchian المرفوعة من نسخة محرك أخرى؛ **محرَّمة المقارنة** مع adaptive_trend حتى يُعاد تشغيلها (L0001). **العزل: D‑0008.**

## 6) الفائز «الرسمي» يقع داخل تعادل دقيق

أفضل net على الاختبار `-161.46831478329625` يظهر مرّتين في `sweep_test.csv`: `(8.0,1.2,1440,6,0,0,0)` و`(...,0,1,0)`. المحرك يحسم بـ`sort_values("net").head(1)` بلا كسر تعادل مُعرَّف ⇒ الاختيار ليس دليلاً على أفضلية V3B (وهو خامل أصلاً، §2). في donchian الحالة أسوأ: القمة مشتركة بين **4** نهائيين.

## ما لم يتغيّر (وهو الأهم)

الاستنتاج العلمي كما وُثّق، بل أقوى: **كل** النهائيين في المسارين سلبيون خارج العينة ⇒ لا توجد أي تركيبة صالحة للنشر. الكود لم يُمَسّ، وعينة التحقق تُنتَج في بيئة مختلفة، والمستودع سليم القراءة/الكتابة.
