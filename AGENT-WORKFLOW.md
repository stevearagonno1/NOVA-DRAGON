# طريقة العمل داخل مساحة الوكيل — قانون الميزانية (الدستور، سطر 77)

> **النص الملزم:** «مساحة العمل تحتفظ بنسخ الملفات الخفيفة المهمة فقط.
> وما عداها يُبلغ بالروابط والمسارات المنظمة. النسخة الكاملة لا تنام في
> مساحة العمل بين الجولات.»

## القاعدة
- الدائم في المساحة: **النواة الخفيفة فقط** (`core/` ≈ 21MB): الوثائق · آلة المختبر · الأدوات.
- الثقيل (أرشيف الأسعار 699MB · البيانات · الحزم) **يُجلب عند الحاجة ويُحذف فور الانتهاء**.
- لا نسخة كاملة تبيت بين الجولات — هذا سبب ضياع العمل أربع مرات قبل هذا التصحيح.

## دورة أي جولة قياس
1. اكتب الوحدة داخل `core/history/hyp_lab/` (خفيفة، تبقى).
2. اجلب الأرشيف مؤقتًا في نفس النداء الذي تشغّل فيه القياس.
3. احفظ **المخرجات المختصرة فقط** (جداول النتائج) داخل النواة.
   ملفات الصفقات الخام تُرفع إلى الخزنة مباشرة ولا تُترك في المساحة.
4. التزم وارفع إلى فرع الجلسة **في نفس النداء**.
5. احذف الثقيل قبل انتهاء الدور.

## المحظور
- `git clone` كامل بلا `--sparse` أو `--filter`.
- ترك `crypto_archive/` أو `data/` أو أي `*.parquet` في المساحة بعد انتهاء النداء.
- تأجيل الرفع إلى نداء لاحق — ما لم يُرفع قد لا يوجد غدًا.
