# 🤖 bot/ — البوت الموحَّد القابل للتشغيل (الدفعة ٤ — 2026-09-16 · القرار `D‑0024`)

> **ملف الدخول الثابت:** `bot/NOVA.py` — لربط Render لاحقاً (النشر = `BACKLOG`، ليس الآن).

## الهوية

| البند | القيمة |
|---|---|
| الإصدار | **V7.0.1 النهائي** — آخر إصدار موحّد (إصلاح ذاكرة التنقيط + تدقيقات ٣٢–٣٥) |
| الحجم | 383,374 بايت · 7,230 سطراً · 262 دالة |
| البصمة الكاملة | `sha256 = 3e08f1f8f103d391207226d5973add13d4d91c52ad8f7911f5f221de39e79234` |
| `ast.parse` | سليم ✓ |
| جروح `__REDACTED__` | **0** — نظيف تماماً |

## النسب الكاملة

`history/clean/NOVA_bin_pending/جميع اصدارات البوت/1/NOVA.Finally_X/NOVA.py`
(مستخرج من `1/NOVA.Finally.zip` داخل الحزمة الأم — بصمتها في `new/CENSUS.md` وشهادة النقي
`history/clean/MANIFEST-CLEAN.md`) ← **نسخة حرفية بلا أي تعديل منطقي** (البند الدستوري ١١).
الإثبات التاريخي الكامل: `new/CROWN-CHECK.md` — فرق التاج عن نسخة المستودع المجروحة
(`history/bot_versions/1/NOVA (3).py`) = الجروح الأربعة فقط، في مواضع مفاتيح/إرجاع معروفة.

## التنبيهات الملزمة

1. **لا ذكاء اصطناعي يقترب من البوت** (حكم المستخدم النهائي): كود دماغ Gemini موجود في الملف
   لكنه معطَّل بعدم وجود مفتاح بيئة — ويبقى معطلاً؛ لا يُفعَّل ولا يُعدَّل إلا بقرار موثق من المستخدم.
2. المفاتيح تُقرأ من متغيرات البيئة (`.env`) — لا مفتاح في الكود، ولا يدخل مفتاح إلى git أبداً.
3. أي تعديل مستقبلي على `bot/` = سطر قرار موثق أولاً (البند ١١) — لا استثناءات «تحسينية».

## إعادة الاشتقاق (أمر واحد من جذر المستودع)

```
cmp bot/NOVA.py "history/clean/NOVA_bin_pending/جميع اصدارات البوت/1/NOVA.Finally_X/NOVA.py" \
  && sha256sum bot/NOVA.py
# البصمة المتوقعة: 3e08f1f8f103d391207226d5973add13d4d91c52ad8f7911f5f221de39e79234
```
