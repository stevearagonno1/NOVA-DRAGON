# 🔍 شهادة فحص جهوزية البوت — bot/READINESS-CHECK.md

> **التاريخ:** 2026-09-16 · **القرار:** `D‑0026` · **المفحوص:** `bot/NOVA.py`
> (بصمة `3e08f1f8f103d391207226d5973add13d4d91c52ad8f7911f5f221de39e79234`)
> **طبيعة الفحص:** بنيوي ثابت (static) بلا مفاتيح وبلا شبكة وبلا تشغيل فعلي —
> الكود لم يُمس بحرف واحد (البند ١١).

## ١) البنية

| الفحص | النتيجة |
|---|---|
| `ast.parse` | **سليم ✓** |
| الدوال · الأسطر · الحجم | 262 · 7,230 · 383,374 بايت — مطابق للتاج |
| جروح `__REDACTED__` | **0** |
| نقطة الدخول `if __name__ == "__main__"` | **موجودة ✓** (ملف دخول صالح لـRender لاحقاً) |

## ٢) المكتبات الخارجية المطلوبة (٤ فقط)

`aiohttp` · `numpy` · `pandas` · `google` (حزمة `google-generativeai` — **اختيارية**:
لا تُستورد فعلياً إلا عند وجود مفتاح، والتعطل آمن بدونها).
⇒ التشغيل الفعلي يحتاج الثلاثة الأولى فقط.

## ٣) الحواجز الحديدية داخل الكود (الدستور §٨) — كلها سليمة ✅

| الحاجز | الشاهد من الكود |
|---|---|
| **لا رافعة** | صفر إشارة لـ`leverage`/`set_leverage`/`margin_type` |
| **لا بيع على المكشوف** | صفر إشارة لـ`short_sell`؛ أوامر `SELL` الثلاث كلها إغلاق مركز سبوت (بيع ما نملكه) |
| **لا سحب تلقائي** | **صفر** إشارة لـ`withdraw` أو مسار السحب في API — لا يستطيع الكود سحب أي دولار |
| **بوابة التداول الحي** | `LIVE_TRADING_CONFIRM` متغير بيئة مستقل — لا حي بلا تأكيد صريح |
| **الافتراض الآمن** | `BINANCE_ENV` افتراضه **`testnet`** — الحي لا يبدأ بالصدفة |

## ٤) وضع الذكاء الاصطناعي (حكم المستخدم: لا AI يقترب من البوت)

`GEMINI_API_KEY` يُقرأ من البيئة، والدالة المهيّئة **تتعطل آمنة** عند غيابه
(`if not GEMINI_API_KEY: ...` — سطر ~4607). بلا مفتاح في البيئة = الدماغ معطَّل تماماً.
**القرار القائم: يبقى بلا مفتاح.**

## ٥) متغيرات البيئة العشرة

`BINANCE_API_KEY` · `BINANCE_API_SECRET` · `BINANCE_ENV` · `LIVE_TRADING_CONFIRM` ·
`TELEGRAM_BOT_TOKEN` · `TELEGRAM_CHAT_ID` · `GEMINI_API_KEY` · `GEMINI_MODEL` ·
`UNIVERSE_MODE` · `BTC_CONTEXT_SYMBOL`
— لا مفتاح واحد مكتوب داخل الكود ✓

## ٦) ما لم يُفحص (صدق الأرقام — البند ١٠)

التشغيل الفعلي وربط الشبكة والأداء المالي: **غير مقاس** (لا مفاتيح ولا شبكة في هذه البيئة،
ولا إذن تشغيل حي). هذه الشهادة تثبت **سلامة البنية والحواجز** فقط — لا تُقرأ كإذن نشر.
بوابة القبول الحي بشروطها السبعة في `live/LIVE-TRADING-RULES.md` هي الحكم.

## ٧) إعادة الاشتقاق (أمر واحد)

```
python3 -c "import ast;s=open('bot/NOVA.py',encoding='utf-8').read();ast.parse(s);\
print('AST OK',s.count('__REDACTED__'),'جروح')" && \
grep -ciE 'withdraw|leverage|short_sell' bot/NOVA.py   # المتوقع: 0
```
